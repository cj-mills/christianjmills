<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.0.0">Jekyll</generator><link href="https://christianjmills.com/feed.xml" rel="self" type="application/atom+xml" /><link href="https://christianjmills.com/" rel="alternate" type="text/html" /><updated>2020-11-30T14:25:54-06:00</updated><id>https://christianjmills.com/feed.xml</id><title type="html">Christian Mills</title><subtitle>My personal blog</subtitle><entry><title type="html">Barracuda PoseNet Tutorial Pt. 8</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-8/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 8" /><published>2020-11-20T00:00:00-06:00</published><updated>2020-11-20T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-8</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-8/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 21, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-7&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-7/&quot;&gt;Part 7&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#add-unityenginevideo-namespace&quot;&gt;Add UnityEngine.Video Namespace&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#add-video-resolution-variables&quot;&gt;Add Video Resolution Variables&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#update-start-method&quot;&gt;Update Start() Method&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#update-processoutput-method&quot;&gt;Update ProcessOutput() Method&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#try-it-out&quot;&gt;Try It Out&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;add-unityenginevideo-namespace&quot;&gt;Add &lt;code class=&quot;highlighter-rouge&quot;&gt;UnityEngine.Video&lt;/code&gt; Namespace&lt;/h2&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script and add a new &lt;a href=&quot;https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/using-directive&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;using directive&lt;/code&gt;&lt;/a&gt; at the top. We need the &lt;code class=&quot;highlighter-rouge&quot;&gt;UnityEngine.Video&lt;/code&gt; namespace to work with the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; object.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\import_unityengine_video.png&quot; alt=&quot;import_unityengine_video&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;add-webcam-variables&quot;&gt;Add Webcam Variables&lt;/h2&gt;

&lt;p&gt;Unity defaults to a resolution of &lt;code class=&quot;highlighter-rouge&quot;&gt;640 x 480&lt;/code&gt; for webcams. There is no built-in method that returns the frame rate. However, the default does not appear to be over 30fps.  We can request a resolution and frame rate when initializing the &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt;. Unity should accept the requested settings as long as the camera supports them. Unity defaults to its closest preset if the requested settings are not supported.&lt;/p&gt;

&lt;h3 id=&quot;create-webcamheight-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamHeight&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Add a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;int&lt;/code&gt; variable so we can adjust the camera height from the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamHeight&lt;/code&gt;. My webcam supports 720p at 60fps, so I’ve set the default value to &lt;code class=&quot;highlighter-rouge&quot;&gt;720&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\webcamHeight_variable.png&quot; alt=&quot;webcamHeight_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-webcamwidth-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamWidth&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Next, create a variable for the camera’s width and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamWidth&lt;/code&gt;. I’ve set the default value to &lt;code class=&quot;highlighter-rouge&quot;&gt;1280&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\webcamWidth_variable.png&quot; alt=&quot;webcamWidth_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-webcamfps-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamFPS&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We’ll also add a variable to set the frame rate for the camera and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamFPS&lt;/code&gt;. Set the default value to &lt;code class=&quot;highlighter-rouge&quot;&gt;60&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\webcam_fps_variable.png&quot; alt=&quot;webcam_fps_variable&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;add-video-resolution-variables&quot;&gt;Add Video Resolution Variables&lt;/h2&gt;

&lt;p&gt;Next, we need to create a couple of private &lt;code class=&quot;highlighter-rouge&quot;&gt;int&lt;/code&gt; variables to store the dimensions of the video source. Name the variables &lt;code class=&quot;highlighter-rouge&quot;&gt;videoHeight&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;videoWidth&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\video_resolution_variables.png&quot; alt=&quot;video_resolution_variables&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;update-start-method&quot;&gt;Update &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; Method&lt;/h2&gt;

&lt;p&gt;We’ll make some modifications to the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method to prepare for input with different aspect ratios.&lt;/p&gt;

&lt;h3 id=&quot;get-reference-to-video-player&quot;&gt;Get Reference to &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;Create a new &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; variable to access the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; object. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;videoPlayer&lt;/code&gt; and place it at the top of the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method. We’ll also move the &lt;code class=&quot;highlighter-rouge&quot;&gt;Transform videoScreen&lt;/code&gt; variable outside of the &lt;code class=&quot;highlighter-rouge&quot;&gt;if (useWebcam)&lt;/code&gt; statement.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\find_video_player.png&quot; alt=&quot;find_video_player&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;get-webcam-resolution&quot;&gt;Get Webcam Resolution&lt;/h3&gt;

&lt;p&gt;We can’t access the webcam resolution until after the webcam has started. Therefore, we’ll update the values for &lt;code class=&quot;highlighter-rouge&quot;&gt;videoHeight&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;videoWidth&lt;/code&gt; after &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture.Play()&lt;/code&gt; has been called.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\get_webcam_resolution.png&quot; alt=&quot;get_webcam_resolution&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;get-video-clip-dimensions&quot;&gt;Get Video Clip Dimensions&lt;/h3&gt;

&lt;p&gt;We need to get the video resolution from the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; object when we’re not using a webcam. We can get the height and width by accessing the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoPlayer&lt;/code&gt; component.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\get_video_clip_dimensions.png&quot; alt=&quot;get_video_clip_dimensions&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;replace-videotexture&quot;&gt;Replace &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;The dimensions for &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; can’t be updated once it’s been created. We’ll need to replace it with a new &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderTexture&lt;/code&gt; that uses the new &lt;code class=&quot;highlighter-rouge&quot;&gt;videoHeight&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;videoWidth&lt;/code&gt; values. We also need to set the new &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; as the &lt;code class=&quot;highlighter-rouge&quot;&gt;targetTexture&lt;/code&gt; for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; object.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\replace_videoTexture.png&quot; alt=&quot;replace_videoTexture&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;update-videoscreen&quot;&gt;Update &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;Next, we need to apply the new &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; to the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; object. We’ll also resize and reposition the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; to account for any changes in the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; dimensions.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\update_videoScreen_2.png&quot; alt=&quot;update_videoScreen&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;adjust-main-camera&quot;&gt;Adjust &lt;code class=&quot;highlighter-rouge&quot;&gt;Main Camera&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;We’ll also resize and reposition the &lt;code class=&quot;highlighter-rouge&quot;&gt;Main Camera&lt;/code&gt; object to account for changes to the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\adjust_main_camera.png&quot; alt=&quot;adjust_main_camera&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;update-processoutput-method&quot;&gt;Update &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt; Method&lt;/h2&gt;

&lt;p&gt;Finally, we need to modify how we calculate the key point locations.&lt;/p&gt;

&lt;h3 id=&quot;update-scaling-calculations&quot;&gt;Update Scaling Calculations&lt;/h3&gt;

&lt;p&gt;We’ll account for different aspect ratios by first determining whether the height or width of &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; is larger. We then use that information to calculate the &lt;code class=&quot;highlighter-rouge&quot;&gt;scale&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;unsqueezeScale&lt;/code&gt; values.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\update_scaling_calculations.png&quot; alt=&quot;update_scaling_calculations&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;update-key-point-calculations&quot;&gt;Update Key Point Calculations&lt;/h3&gt;

&lt;p&gt;We should only scale the &lt;code class=&quot;highlighter-rouge&quot;&gt;xPos&lt;/code&gt; value by the &lt;code class=&quot;highlighter-rouge&quot;&gt;unsqueezeScale&lt;/code&gt; if the width larger than the height. Remove that part from the initial &lt;code class=&quot;highlighter-rouge&quot;&gt;xPos&lt;/code&gt; calculation. We also need to move the &lt;code class=&quot;highlighter-rouge&quot;&gt;if (useWebcam)&lt;/code&gt; statement.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\update_key_point_calculations_part1v2.png&quot; alt=&quot;update_key_point_calculations_part1&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We’ll scale either &lt;code class=&quot;highlighter-rouge&quot;&gt;xPos&lt;/code&gt; or &lt;code class=&quot;highlighter-rouge&quot;&gt;yPos&lt;/code&gt; by &lt;code class=&quot;highlighter-rouge&quot;&gt;unsqueezeScale&lt;/code&gt; depending on whether the width or height value for &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; is larger. Place the &lt;code class=&quot;highlighter-rouge&quot;&gt;if (useWebcam)&lt;/code&gt; statement after this calculation.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\update_key_point_calculations_part2.png&quot; alt=&quot;update_key_point_calculations_part2&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;try-it-out&quot;&gt;Try It Out&lt;/h2&gt;

&lt;p&gt;Now we can test the modifications on some vertical videos from &lt;a href=&quot;https://www.pexels.com/&quot;&gt;Pexels&lt;/a&gt;.&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;a href=&quot;https://www.pexels.com/video/woman-doing-a-jump-rope-exercise-2785536/&quot;&gt;Woman Doing a Jump Rope Exercise&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://www.pexels.com/video/man-dancing-hip-hop-2795742/&quot;&gt;Man Dancing Hip-Hop&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\jump_rope_pose_estimation_4.gif&quot; alt=&quot;jump_rope_pose_estimation_4&quot; /&gt;&lt;/p&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 21, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 7</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-7/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 7" /><published>2020-11-15T00:00:00-06:00</published><updated>2020-11-15T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-7</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-7/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 21, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-6&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-6/&quot;&gt;Part 6&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#modify-posenet-script&quot;&gt;Modify PoseNet Script&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#set-inspector-variable&quot;&gt;Set Inspector Variable&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;modify-posenet-script&quot;&gt;Modify &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; Script&lt;/h2&gt;

&lt;p&gt;We can add the option to use a webcam feed by making some modifications to the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script.&lt;/p&gt;

&lt;h3 id=&quot;create-usewebcam-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;useWebcam&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script and create a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;bool&lt;/code&gt; variable. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;useWebcam&lt;/code&gt; and set the default value to &lt;code class=&quot;highlighter-rouge&quot;&gt;false&lt;/code&gt;. This will create a checkbox in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab that we can use to enable and disable the webcam.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\useWebcam_variable.png&quot; alt=&quot;useWebcam_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-webcamtexture-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We’ll use a &lt;a href=&quot;https://docs.unity3d.com/ScriptReference/WebCamTexture.html&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;WebCamTexture&lt;/code&gt;&lt;/a&gt; variable to store the live video input from our webcam. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\webcamTexture_variable.png&quot; alt=&quot;webcamTexture_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;set-up-webcam-feed&quot;&gt;Set Up Webcam Feed&lt;/h3&gt;

&lt;p&gt;We’ll prepare the webcam feed at the top of the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method.&lt;/p&gt;

&lt;h4 id=&quot;initialize-the-webcamtexture&quot;&gt;Initialize the &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt;&lt;/h4&gt;

&lt;p&gt;First, initialize the &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt;. We’ll use the first video input device Unity finds. If you have more than one webcam attached, you’ll need to &lt;a href=&quot;https://docs.unity3d.com/ScriptReference/WebCamTexture-ctor.html&quot;&gt;specify&lt;/a&gt; the device name.&lt;/p&gt;

&lt;h4 id=&quot;flip-the-videoscreen&quot;&gt;Flip the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt;&lt;/h4&gt;

&lt;p&gt;Next, we need to adjust the rotation and scale of the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; object. The webcam feed doesn’t mirror the user by default. For example, the user’s right arm appears on the left side of the screen. This can be disorienting when looking at the generated pose skeleton. We’ll flip the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; to compensate.&lt;/p&gt;

&lt;h4 id=&quot;start-the-camera&quot;&gt;Start the Camera&lt;/h4&gt;

&lt;p&gt;We’ll use the &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture.Play()&lt;/code&gt; method to start the camera.&lt;/p&gt;

&lt;h4 id=&quot;deactivate-the-video-player&quot;&gt;Deactivate the Video Player&lt;/h4&gt;

&lt;p&gt;Finally, we’ll deactivate the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; as it’s not being used.&lt;/p&gt;

&lt;h4 id=&quot;completed-code&quot;&gt;Completed Code&lt;/h4&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\initialize_webcam_start_method.png&quot; alt=&quot;initialize_webcam_start_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;get-webcamtexture-data&quot;&gt;Get &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt; Data&lt;/h3&gt;

&lt;p&gt;We’ll use the &lt;code class=&quot;highlighter-rouge&quot;&gt;Graphics.Blit()&lt;/code&gt; method to update the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; with the data from &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt;. Add the following code at the top of the &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\useWebcam_update_method.png&quot; alt=&quot;useWebcam_update_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;flip-key-point-locations&quot;&gt;Flip Key Point Locations&lt;/h3&gt;

&lt;p&gt;Flipping the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; does not flip the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; itself. Therefore, the output of the model will not be flipped either. We can fix this by mirroring the &lt;code class=&quot;highlighter-rouge&quot;&gt;xPos&lt;/code&gt; values for the calculated key point locations.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\useWebcam_processOutput_method.png&quot; alt=&quot;useWebcam_processOutput_method&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;set-inspector-variable&quot;&gt;Set Inspector Variable&lt;/h2&gt;

&lt;p&gt;Now we can enable and disable the webcam from the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\enable_useWebcam_inspector.PNG&quot; alt=&quot;enable_useWebcam_inspector&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Note:&lt;/code&gt; Don’t toggle the &lt;code class=&quot;highlighter-rouge&quot;&gt;useWebcam&lt;/code&gt; parameter during runtime with the code as it is.&lt;/p&gt;

&lt;h3 id=&quot;next-part-8&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-8/&quot;&gt;Part 8&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 21, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 6</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-6/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 6" /><published>2020-11-14T00:00:00-06:00</published><updated>2020-11-14T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-6</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-6/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 24, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-5&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-5/&quot;&gt;Part 5&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#create-drawskeleton-script&quot;&gt;Create DrawSkeleton Script&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-skeletonrenderer&quot;&gt;Create SkeletonRenderer&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#try-it-out&quot;&gt;Try It Out&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;create-drawskeleton-script&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;DrawSkeleton&lt;/code&gt; Script&lt;/h2&gt;

&lt;p&gt;We’ll complete our pose skeleton by drawing lines connecting the appropriate key points. Create a new &lt;code class=&quot;highlighter-rouge&quot;&gt;C#&lt;/code&gt; script and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;DrawSkeleton&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_drawSkeleton_script.PNG&quot; alt=&quot;create_drawSkeleton_script&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-keypoints-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;keypoints&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We need to access the key point objects so make another public &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; array just like in the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\keypoints_variable_drawSkeleton.png&quot; alt=&quot;keypoints_variable_drawSkeleton&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-lines-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;lines&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Next, create a private &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; array to hold the lines themselves. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;lines&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\lines_variable.png&quot; alt=&quot;lines_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-linerenderers-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;lineRenderers&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We’ll use &lt;a href=&quot;https://docs.unity3d.com/Manual/class-LineRenderer.html&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;LineRenderer&lt;/code&gt;&lt;/a&gt; components to draw the skeleton.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\lineRenderers_variable.png&quot; alt=&quot;lineRenderers_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-jointpairs-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;jointPairs&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;The next variable will contain pairs of key point indices. The corresponding key points indicate the start and end points for the skeleton lines.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\jointPairs_variable.png&quot; alt=&quot;jointPairs_variable&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;joint-pairs&quot;&gt;Joint Pairs&lt;/h4&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;#&lt;/th&gt;
      &lt;th&gt;Starting Key Point&lt;/th&gt;
      &lt;th&gt;Ending Key Point&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;1&lt;/td&gt;
      &lt;td&gt;Nose&lt;/td&gt;
      &lt;td&gt;Left Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;2&lt;/td&gt;
      &lt;td&gt;Nose&lt;/td&gt;
      &lt;td&gt;Right Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;3&lt;/td&gt;
      &lt;td&gt;Left Eye&lt;/td&gt;
      &lt;td&gt;Left Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;4&lt;/td&gt;
      &lt;td&gt;Right Eye&lt;/td&gt;
      &lt;td&gt;Right Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;5&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;6&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;7&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;8&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;9&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;10&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;11&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
      &lt;td&gt;Left Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;12&lt;/td&gt;
      &lt;td&gt;Left Elbow&lt;/td&gt;
      &lt;td&gt;Left Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;13&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
      &lt;td&gt;Right Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;14&lt;/td&gt;
      &lt;td&gt;Right Elbow&lt;/td&gt;
      &lt;td&gt;Right Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;15&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
      &lt;td&gt;Left Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;16&lt;/td&gt;
      &lt;td&gt;Left Knee&lt;/td&gt;
      &lt;td&gt;Left Ankle&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;17&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
      &lt;td&gt;Right Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;18&lt;/td&gt;
      &lt;td&gt;Right Knee&lt;/td&gt;
      &lt;td&gt;Right Ankle&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;h3 id=&quot;create-linewidth-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;lineWidth&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;The last variable we’ll make defines the line width.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\lineWidth_variable.png&quot; alt=&quot;lineWidth_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;initialize-variables&quot;&gt;Initialize Variables&lt;/h3&gt;

&lt;p&gt;We need to initialize the &lt;code class=&quot;highlighter-rouge&quot;&gt;lines&lt;/code&gt;, &lt;code class=&quot;highlighter-rouge&quot;&gt;lineRenderers&lt;/code&gt;, and &lt;code class=&quot;highlighter-rouge&quot;&gt;jointPairs&lt;/code&gt; variables in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\initialize_drawSkeleton_variables.png&quot; alt=&quot;initialize_drawSkeleton_variables&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-initializeline-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;InitializeLine()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;We’ll create a new method to set up each of the lines in the pose skeleton. The method will create an empty &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; for a line and add a &lt;code class=&quot;highlighter-rouge&quot;&gt;LineRenderer&lt;/code&gt; component to it. We won’t set the start and end positions as none of the key points will have updated yet.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\initializeLine_method.png&quot; alt=&quot;initializeLine_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-initializeskeleton-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;InitializeSkeleton()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;Next, we need to call &lt;code class=&quot;highlighter-rouge&quot;&gt;InitializeLine()&lt;/code&gt; in a new method for each line in the pose skeleton. We’ll give each region of the skeleton a different color.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\initializeSkeleton_method.png&quot; alt=&quot;initializeSkeleton_method&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;call-the-method&quot;&gt;Call the method&lt;/h4&gt;

&lt;p&gt;We’ll initialize the pose skeleton lines in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\call_initializeSkeleton_method.png&quot; alt=&quot;call_initializeSkeleton_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-renderskeleton-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderSkeleton()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;The last method we need to define will handle updating the position of the each of the lines in the pose skeleton. The method will iterate through each of the joint pairs and update the start and end positions for the associated &lt;code class=&quot;highlighter-rouge&quot;&gt;LineRenderer&lt;/code&gt;. We’ll only display a given line if both of the key point objects are currently active.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\renderSkeleton_method.png&quot; alt=&quot;renderSkeleton_method&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;call-the-method-1&quot;&gt;Call the method&lt;/h4&gt;

&lt;p&gt;We’ll render the skeleton lines in the &lt;code class=&quot;highlighter-rouge&quot;&gt;LateUpdate()&lt;/code&gt; method instead of &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt;. This will ensure the PoseNet model has run for the latest frame before updating the pose skeleton.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\call_renderSkeleton_method_2.png&quot; alt=&quot;call_renderSkeleton_method&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;create-skeletonrenderer&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;SkeletonRenderer&lt;/code&gt;&lt;/h2&gt;

&lt;p&gt;We’ll attach the &lt;code class=&quot;highlighter-rouge&quot;&gt;DrawSkeleton&lt;/code&gt; script to a new &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt;. Create an empty &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;SkeletonRenderer&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_skeletonRenderer_object.PNG&quot; alt=&quot;create_skeletonRenderer_object&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;attach-the-drawskeleton-script&quot;&gt;Attach the &lt;code class=&quot;highlighter-rouge&quot;&gt;DrawSkeleton&lt;/code&gt; Script&lt;/h3&gt;

&lt;p&gt;With &lt;code class=&quot;highlighter-rouge&quot;&gt;SkeletonRenderer&lt;/code&gt; selected in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt;, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;DrawSkeleton&lt;/code&gt; script into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\skeletonRenderer_inspector_empty.PNG&quot; alt=&quot;skeletonRenderer_inspector_empty&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;assign-key-points&quot;&gt;Assign Key Points&lt;/h3&gt;

&lt;p&gt;Drag and drop the key point objects onto the &lt;code class=&quot;highlighter-rouge&quot;&gt;Keypoints&lt;/code&gt; parameter just like with the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\skeletonRenderer_inspector_full.PNG&quot; alt=&quot;skeletonRenderer_inspector_full&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;try-it-out&quot;&gt;Try It Out&lt;/h2&gt;

&lt;p&gt;If you press the play button, you should see something like this.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th&gt;Variable&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;Value&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;imageHeight&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;720&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;imageWidth&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;720&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;minConfidence&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;85&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\pose_skeleton_480p_90c.gif&quot; alt=&quot;pose_skeleton_480p_90c&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;next-part--7&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-7/&quot;&gt;Part  7&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 24, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 5</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-5/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 5" /><published>2020-11-14T00:00:00-06:00</published><updated>2020-11-14T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-5</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-5/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 24, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-4&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-4/&quot;&gt;Part 4&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#create-key-points&quot;&gt;Create Key Points&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#map-key-point-locations&quot;&gt;Map Key Point Locations&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;create-key-points&quot;&gt;Create Key Points&lt;/h2&gt;

&lt;p&gt;We need to create a separate &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; for each of the &lt;code class=&quot;highlighter-rouge&quot;&gt;17&lt;/code&gt; key points.&lt;/p&gt;

&lt;h3 id=&quot;create-container&quot;&gt;Create Container&lt;/h3&gt;

&lt;p&gt;In the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab, create an empty &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;Key Points&lt;/code&gt;. We’ll store the key point objects in here to keep things organized.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Optional:&lt;/strong&gt; With &lt;code class=&quot;highlighter-rouge&quot;&gt;Key Points&lt;/code&gt; selected, right-click the &lt;code class=&quot;highlighter-rouge&quot;&gt;Transform&lt;/code&gt; component in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. Click &lt;code class=&quot;highlighter-rouge&quot;&gt;Reset&lt;/code&gt; in the pop-up menu. This will reset the object’s position to the origin.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\reset_transform.PNG&quot; alt=&quot;reset_transform&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-gameobjects&quot;&gt;Create GameObjects&lt;/h3&gt;

&lt;p&gt;Right-click the &lt;code class=&quot;highlighter-rouge&quot;&gt;Key Points&lt;/code&gt; object and select &lt;code class=&quot;highlighter-rouge&quot;&gt;Sphere&lt;/code&gt; under &lt;code class=&quot;highlighter-rouge&quot;&gt;3D Object&lt;/code&gt;. This will create a nested &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; inside &lt;code class=&quot;highlighter-rouge&quot;&gt;Key Points&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_keypoint_gameobject.PNG&quot; alt=&quot;create_keypoint_gameobject&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Select the new &lt;code class=&quot;highlighter-rouge&quot;&gt;Sphere&lt;/code&gt; object and press Ctrl-d to duplicate it. We’ll need &lt;code class=&quot;highlighter-rouge&quot;&gt;17&lt;/code&gt; spheres total.&lt;/p&gt;

&lt;p&gt;Rename the &lt;code class=&quot;highlighter-rouge&quot;&gt;Sphere&lt;/code&gt; objects according to the table below.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th&gt;Index&lt;/th&gt;
      &lt;th&gt;Name&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;0&lt;/td&gt;
      &lt;td&gt;Nose&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;1&lt;/td&gt;
      &lt;td&gt;Left Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;2&lt;/td&gt;
      &lt;td&gt;Right Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;3&lt;/td&gt;
      &lt;td&gt;Left Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;4&lt;/td&gt;
      &lt;td&gt;Right Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;5&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;6&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;7&lt;/td&gt;
      &lt;td&gt;Left Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;8&lt;/td&gt;
      &lt;td&gt;Right Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;9&lt;/td&gt;
      &lt;td&gt;Left Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;10&lt;/td&gt;
      &lt;td&gt;Right Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;11&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;12&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;13&lt;/td&gt;
      &lt;td&gt;Left Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;14&lt;/td&gt;
      &lt;td&gt;Right Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;15&lt;/td&gt;
      &lt;td&gt;Left Ankle&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;16&lt;/td&gt;
      &lt;td&gt;Right Ankle&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;h4 id=&quot;result&quot;&gt;Result&lt;/h4&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\keypoint_gameobjects.PNG&quot; alt=&quot;keypoint_gameobjects&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;resize-gameobjects&quot;&gt;Resize GameObjects&lt;/h3&gt;

&lt;p&gt;Next, we’ll make the key point objects larger so that they’re easier to see. Select the &lt;code class=&quot;highlighter-rouge&quot;&gt;Nose&lt;/code&gt; object in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierachy&lt;/code&gt;. Then,   hold Shift and click &lt;code class=&quot;highlighter-rouge&quot;&gt;RightAnkle&lt;/code&gt; to select all 17 objects at once.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\select_all_keypoint_objects.PNG&quot; alt=&quot;select_all_keypoint_objects&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We need to increase the &lt;code class=&quot;highlighter-rouge&quot;&gt;X&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;Y&lt;/code&gt; values for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Scale&lt;/code&gt; parameter in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. Increasing them to 10 should be enough.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\increase_key_point_object_size.PNG&quot; alt=&quot;increase_key_point_object_size&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;change-gameobject-material&quot;&gt;Change GameObject Material&lt;/h3&gt;

&lt;p&gt;The default color for a &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; doesn’t stand out much against the background. We’ll make the key point objects yellow since it’s apparently really easy for humans to spot.&lt;/p&gt;

&lt;h4 id=&quot;create-yellow-material&quot;&gt;Create Yellow Material&lt;/h4&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;Materials&lt;/code&gt; folder in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Assets&lt;/code&gt; window. Right-click an empty space and select &lt;code class=&quot;highlighter-rouge&quot;&gt;Material&lt;/code&gt; in the the &lt;code class=&quot;highlighter-rouge&quot;&gt;Create&lt;/code&gt; sub-menu. Name the new material &lt;code class=&quot;highlighter-rouge&quot;&gt;Yellow&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_material.PNG&quot; alt=&quot;create_material&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;change-material-color&quot;&gt;Change Material Color&lt;/h4&gt;

&lt;p&gt;With the &lt;code class=&quot;highlighter-rouge&quot;&gt;Yellow&lt;/code&gt; material selected, click the small white box in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. A &lt;code class=&quot;highlighter-rouge&quot;&gt;Color&lt;/code&gt; window should pop up.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\select_material_color_3.png&quot; alt=&quot;select_material_color&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Set the value for &lt;code class=&quot;highlighter-rouge&quot;&gt;B&lt;/code&gt; to &lt;code class=&quot;highlighter-rouge&quot;&gt;0&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Color&lt;/code&gt; window. This will change the color to pure yellow.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\change_material_color_to_yellow.PNG&quot; alt=&quot;change_material_color_to_yellow&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;make-material-unlit&quot;&gt;Make Material Unlit&lt;/h4&gt;

&lt;p&gt;We’ll change the &lt;code class=&quot;highlighter-rouge&quot;&gt;Shader&lt;/code&gt; for the material to &lt;code class=&quot;highlighter-rouge&quot;&gt;Unlit/Color&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\change_material_shader_to_unlit_color.PNG&quot; alt=&quot;change_material_shader_to_unlit_color&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;assign-yellow-material&quot;&gt;Assign Yellow Material&lt;/h4&gt;

&lt;p&gt;Select all the key point objects in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab. Then, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;Yellow&lt;/code&gt; material into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\assign_yellow_material.PNG&quot; alt=&quot;assign_yellow_material&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;map-key-point-locations&quot;&gt;Map Key Point Locations&lt;/h2&gt;

&lt;p&gt;Now we can update the positions of the key point objects using the location data obtained from the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; model. Ordinarily, we would implement this in a separate &lt;code class=&quot;highlighter-rouge&quot;&gt;C#&lt;/code&gt; script. This script would access the &lt;code class=&quot;highlighter-rouge&quot;&gt;keypointLocations[][]&lt;/code&gt; array in the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script. However, we’ll do it in the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script to keep things simple.&lt;/p&gt;

&lt;h3 id=&quot;create-keypoints-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;keypoints&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script and add a public &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; array. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;keypoints&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\keypoints_variable.png&quot; alt=&quot;keypoints_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;assign-the-key-point-objects&quot;&gt;Assign the Key Point Objects&lt;/h3&gt;

&lt;p&gt;Select the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseEstimator&lt;/code&gt; object in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab. Then, click the small lock icon above the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. This will lock the current selected object in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\lock_inspector_2.png&quot; alt=&quot;lock_inspector_2&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Make sure the &lt;code class=&quot;highlighter-rouge&quot;&gt;Size&lt;/code&gt; value for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Keypoints&lt;/code&gt; variable is set to &lt;code class=&quot;highlighter-rouge&quot;&gt;0&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\initialize_keypoints_parameter.png&quot; alt=&quot;initialize_keypoints_parameter&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Select all the key point objects in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt;. Then, drag and drop them onto the &lt;code class=&quot;highlighter-rouge&quot;&gt;Keypoints&lt;/code&gt; parameter in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\assign_keypoint_objects.PNG&quot; alt=&quot;assign_keypoint_objects&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Go ahead and unlock the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab by clicking the lock icon again.&lt;/p&gt;

&lt;h3 id=&quot;create-minconfidence-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;minConfidence&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Next, we’ll add a public &lt;code class=&quot;highlighter-rouge&quot;&gt;int&lt;/code&gt; variable. This variable will define the confidence threshold for deciding whether or not to display a given key point object. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;minConfidence&lt;/code&gt; and set the default value to 70. You can add a &lt;code class=&quot;highlighter-rouge&quot;&gt;Range&lt;/code&gt; attribute to create a slider in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. Set the range to &lt;code class=&quot;highlighter-rouge&quot;&gt;[0, 100]&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\minConfidence_variable_2.png&quot; alt=&quot;minConfidence_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-updatekeypointpositions-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;UpdateKeyPointPositions()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;We need to define a new method to update the key point positions. Name the method &lt;code class=&quot;highlighter-rouge&quot;&gt;UpdateKeyPointPositions()&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\updateKeyPointPositions_method_2.png&quot; alt=&quot;updateKeyPointPositions_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;call-the-method&quot;&gt;Call the Method&lt;/h3&gt;

&lt;p&gt;We’ll call the method in &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt; just after &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\call_updateKeyPointPositions_method.png&quot; alt=&quot;call_updateKeyPointPositions_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;next-part-6&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-6/&quot;&gt;Part 6&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 24, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 4</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-4/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 4" /><published>2020-11-12T00:00:00-06:00</published><updated>2020-11-12T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-4</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-4/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 25, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-3&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-3/&quot;&gt;Part 3&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#create-processoutput-method&quot;&gt;Create ProcessOutput() Method&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#calculate-scaling-values&quot;&gt;Calculate Scaling Values&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#locate-key-point-indices&quot;&gt;Locate Key Point Indices&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#calculate-key-point-positions&quot;&gt;Calculate Key Point Positions&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;create-processoutput-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt; Method&lt;/h2&gt;

&lt;p&gt;The post processing steps will be handled in a new method called &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt;. The method will take in the output &lt;code class=&quot;highlighter-rouge&quot;&gt;Tensors&lt;/code&gt; from the &lt;code class=&quot;highlighter-rouge&quot;&gt;predictionLayer&lt;/code&gt; and the &lt;code class=&quot;highlighter-rouge&quot;&gt;offsetsLayer&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\processoutput_method_empty.png&quot; alt=&quot;processoutput_method_empty&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Before filling out the function, we need to create a new constant and a new variable.&lt;/p&gt;

&lt;h3 id=&quot;create-numkeypoints-constant&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;numKeypoints&lt;/code&gt; Constant&lt;/h3&gt;

&lt;p&gt;The PoseNet model estimates the 2D locations of &lt;code class=&quot;highlighter-rouge&quot;&gt;17&lt;/code&gt; key points on a human body.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th&gt;Index&lt;/th&gt;
      &lt;th&gt;Name&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;0&lt;/td&gt;
      &lt;td&gt;Nose&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;1&lt;/td&gt;
      &lt;td&gt;Left Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;2&lt;/td&gt;
      &lt;td&gt;Right Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;3&lt;/td&gt;
      &lt;td&gt;Left Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;4&lt;/td&gt;
      &lt;td&gt;Right Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;5&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;6&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;7&lt;/td&gt;
      &lt;td&gt;Left Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;8&lt;/td&gt;
      &lt;td&gt;Right Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;9&lt;/td&gt;
      &lt;td&gt;Left Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;10&lt;/td&gt;
      &lt;td&gt;Right Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;11&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;12&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;13&lt;/td&gt;
      &lt;td&gt;Left Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;14&lt;/td&gt;
      &lt;td&gt;Right Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;15&lt;/td&gt;
      &lt;td&gt;Left Ankle&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;16&lt;/td&gt;
      &lt;td&gt;Right Ankle&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Since the number of key points never changes, we’ll store it in an &lt;code class=&quot;highlighter-rouge&quot;&gt;int&lt;/code&gt; constant. Name the constant &lt;code class=&quot;highlighter-rouge&quot;&gt;numKeypoints&lt;/code&gt; and set the value to &lt;code class=&quot;highlighter-rouge&quot;&gt;17&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\numKeypoints_constant.png&quot; alt=&quot;numKeypoints_constant&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-keypointlocations-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;keypointLocations&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;The processed output from the model will be stored in a new variable called &lt;code class=&quot;highlighter-rouge&quot;&gt;keypointLocations&lt;/code&gt;. This variable will contain the &lt;code class=&quot;highlighter-rouge&quot;&gt;(x,y)&lt;/code&gt; coordinates for each key point. For this tutorial, the coordinates will be scaled to the original resolution of &lt;code class=&quot;highlighter-rouge&quot;&gt;1920x1080&lt;/code&gt; for &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;This variable will also store the confidence values associated with the coordinates. The model predicts key point locations even when there isn’t a human in the input image. In such situations, the confidence values will likely be quite low. We can decide how to handle the latest coordinates based on a confidence threshold that we pick.&lt;/p&gt;

&lt;p&gt;There are many ways we can store this information. For simplicity, we’ll stick with an array of arrays. The array will have &lt;code class=&quot;highlighter-rouge&quot;&gt;17&lt;/code&gt; elements. Each element will contain the location information for the key point that matches their index.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\keypointLocations_variable.png&quot; alt=&quot;numKeyPoints_and_keypointLocations&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;retrieve-output-tenors&quot;&gt;Retrieve Output Tenors&lt;/h3&gt;

&lt;p&gt;Call &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt; after &lt;code class=&quot;highlighter-rouge&quot;&gt;engine.Execute(input)&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt;  method. We’ll use the &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.IWorker.html#Unity_Barracuda_IWorker_PeekOutput_System_String_&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;engine.PeekOutput()&lt;/code&gt;&lt;/a&gt; method to get a reference to the output &lt;code class=&quot;highlighter-rouge&quot;&gt;Tensors&lt;/code&gt; from the model. Since they are just references, we don’t need to manually dispose of them.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\update_method_processoutput.png&quot; alt=&quot;update_method_processoutput&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Now we can start filling out the &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt; method.&lt;/p&gt;

&lt;h2 id=&quot;calculate-scaling-values&quot;&gt;Calculate Scaling Values&lt;/h2&gt;

&lt;p&gt;The heatmaps generated by the model are much smaller than the input image fed into it. We’ll need to make some calculations to accurately scale the key point locations back up to the source resolution.&lt;/p&gt;

&lt;h3 id=&quot;calculate-model-stride&quot;&gt;Calculate Model Stride&lt;/h3&gt;

&lt;p&gt;The heatmap dimensions are dependent on both the size of the input image and a fixed integer value called the stride. The stride determines how much smaller the heatmaps will be than the input image. The model used in this tutorial has a stride of &lt;code class=&quot;highlighter-rouge&quot;&gt;32&lt;/code&gt;. The heatmap dimensions are equal to the &lt;a href=&quot;https://www.mathsisfun.com/sets/function-floor-ceiling.html&quot;&gt;ceiling&lt;/a&gt; of &lt;code class=&quot;highlighter-rouge&quot;&gt;resolution/stride&lt;/code&gt;. With our default input resolution of &lt;code class=&quot;highlighter-rouge&quot;&gt;360 x 360&lt;/code&gt;, the size of the heatmaps are &lt;code class=&quot;highlighter-rouge&quot;&gt;12 x 12&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;Since we know the stride for this model, we could make it a constant value. However, calculating it is an easy way to make sure. This also makes it less of a hassle when switching between models with different stride values.&lt;/p&gt;

&lt;h4 id=&quot;model-with-a-different-stride-value&quot;&gt;Model with a Different Stride Value&lt;/h4&gt;

&lt;ul&gt;
  &lt;li&gt;ResNet50 Stride 16: (&lt;a href=&quot;https://drive.google.com/file/d/1dlsWlBpjgD2AuZgi-qhZs-1IV-T98iLM/view?usp=sharing&quot;&gt;download&lt;/a&gt;)&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;To get the stride value, we’ll select a dimension of &lt;code class=&quot;highlighter-rouge&quot;&gt;inputImage&lt;/code&gt; and subtract &lt;code class=&quot;highlighter-rouge&quot;&gt;1&lt;/code&gt;. We then divide that value by the same dimension of the heatmap with &lt;code class=&quot;highlighter-rouge&quot;&gt;1&lt;/code&gt; subtracted as well. If we don’t subtract &lt;code class=&quot;highlighter-rouge&quot;&gt;1&lt;/code&gt;, we’ll undershoot the stride value.&lt;/p&gt;

&lt;p&gt;For most input resolutions this will yield a value that is slightly above the actual stride. If we left it there, the key point locations would be offset from the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;. To compensate, we’ll subtract the remainder of the calculated stride divided by &lt;code class=&quot;highlighter-rouge&quot;&gt;8&lt;/code&gt;. The stride for the PoseNet models provided in this tutorial series are all multiples of &lt;code class=&quot;highlighter-rouge&quot;&gt;8&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\stride.png&quot; alt=&quot;stride&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;calculate-image-scale&quot;&gt;Calculate Image Scale&lt;/h3&gt;

&lt;p&gt;After scaling the output back to the &lt;code class=&quot;highlighter-rouge&quot;&gt;inputImage&lt;/code&gt; resolution, we’ll need to scale the output up to the source resolution. We can use the dimensions of &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; to calculate this scale.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\scale.png&quot; alt=&quot;scale&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;calculate-aspect-ratio-scale&quot;&gt;Calculate Aspect Ratio Scale&lt;/h3&gt;

&lt;p&gt;As I noted in &lt;a href=&quot;https://christianjmills.com/unity/tutorial/2020/11/04/Barracuda-PoseNet-Tutorial-2.html#resize-the-image&quot;&gt;Part 2&lt;/a&gt;, we need to compensate for the change in aspect ratio that results from resizing the image. We can use the dimensions of the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; to stretch the output to the original aspect ratio.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\unsqueezeScale.png&quot; alt=&quot;unsqueezeScale&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;iterate-through-heatmaps&quot;&gt;Iterate Through Heatmaps&lt;/h2&gt;

&lt;p&gt;Now we can iterate through each of the heatmaps and determine the location of the associated key points.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\iterate_through_heatmaps_2.png&quot; alt=&quot;iterate_through_heatmaps&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;locate-key-point-indices&quot;&gt;Locate Key Point Indices&lt;/h3&gt;

&lt;p&gt;For each heatmap, we’ll first need to locate the index with the  highest confidence value. This indicates what region of the image the model thinks is most likely to contain that key point. We’ll create a separate method to handle this.&lt;/p&gt;

&lt;p&gt;The new method will be called &lt;code class=&quot;highlighter-rouge&quot;&gt;LocateKeyPointIndex()&lt;/code&gt; and take in the &lt;code class=&quot;highlighter-rouge&quot;&gt;heatmaps&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;offsets&lt;/code&gt; tensors along with the current &lt;code class=&quot;highlighter-rouge&quot;&gt;keypointIndex&lt;/code&gt;. It will return a &lt;a href=&quot;https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/builtin-types/value-tuples&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Tuple&lt;/code&gt;&lt;/a&gt; containing the &lt;code class=&quot;highlighter-rouge&quot;&gt;(x,y)&lt;/code&gt; coordinates from the heatmap index, the associated offset vector, and the confidence value at the heatmap index.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\locateKeyPointIndex_method.png&quot; alt=&quot;locateKeyPointIndex_method&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;call-the-method&quot;&gt;Call the Method&lt;/h4&gt;

&lt;p&gt;We’ll call &lt;code class=&quot;highlighter-rouge&quot;&gt;LocateKeyPointIndex()&lt;/code&gt; at the start of each iteration through the for loop in &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\processOutput_locateIndices_2.png&quot; alt=&quot;processOutput_locateIndices&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;calculate-key-point-positions&quot;&gt;Calculate Key Point Positions&lt;/h3&gt;

&lt;p&gt;Now we can calculate the estimated key point locations relative to the source &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;. We’ll first extract the output  from the &lt;code class=&quot;highlighter-rouge&quot;&gt;Tuple&lt;/code&gt; returned by &lt;code class=&quot;highlighter-rouge&quot;&gt;LocateKeyPointIndex()&lt;/code&gt;. The offset vectors are based on the &lt;code class=&quot;highlighter-rouge&quot;&gt;inputImage&lt;/code&gt; resolution so we’ll scale the &lt;code class=&quot;highlighter-rouge&quot;&gt;(x,y)&lt;/code&gt; coordinates by the &lt;code class=&quot;highlighter-rouge&quot;&gt;stride&lt;/code&gt; before adding them. We’ll then scale the coordinates up to the source &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;Only the x-axis position is scaled by the &lt;code class=&quot;highlighter-rouge&quot;&gt;unsqueezeValue&lt;/code&gt;. This is specific to our current &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; aspect ratio. I will cover a more dynamic approach in a later post.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\calculate_position_2.png&quot; alt=&quot;calculate_position&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;store-key-point-positions&quot;&gt;Store Key Point Positions&lt;/h4&gt;

&lt;p&gt;Finally, we’ll store the location data for the current key point at the corresponding index in the &lt;code class=&quot;highlighter-rouge&quot;&gt;keypointLocations&lt;/code&gt; array.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\store_position_2.png&quot; alt=&quot;store_position&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;next-part-5&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-5/&quot;&gt;Part 5&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 25, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 3</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-3/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 3" /><published>2020-11-06T00:00:00-06:00</published><updated>2020-11-06T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-3</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-3/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 25, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-2-part-25&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-2/&quot;&gt;Part 2&lt;/a&gt; &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-2-5/&quot;&gt;Part 2.5&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#install-barracuda-package&quot;&gt;Install Barracuda Package&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#import-posenet-model&quot;&gt;Import PoseNet Model&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#load-the-model&quot;&gt;Load the Model&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#set-inspector-variables&quot;&gt;Set Inspector Variables&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#perform-inference&quot;&gt;Perform Inference&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;We can now start using the Barracuda library. We’ll first install the Barracuda package for our project and import the PoseNet model. We’re going to modify the model a bit to make postprocessing easier. Finally, we’ll perform inference using a preprocessed image as input.&lt;/p&gt;

&lt;h2 id=&quot;install-barracuda-package&quot;&gt;Install Barracuda Package&lt;/h2&gt;

&lt;p&gt;Select the &lt;code class=&quot;highlighter-rouge&quot;&gt;Package Manager&lt;/code&gt; tab in the Unity editor.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\select_package_manager_tab.png&quot; alt=&quot;select_package_manager_tab&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Type &lt;code class=&quot;highlighter-rouge&quot;&gt;Barracuda&lt;/code&gt; into the search box. The version of the package used in the tutorial is &lt;code class=&quot;highlighter-rouge&quot;&gt;1.0.4&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\barracuda_search.PNG&quot; alt=&quot;barracuda_search&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Click the &lt;code class=&quot;highlighter-rouge&quot;&gt;Install&lt;/code&gt; button to install the package.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\barracuda_install.PNG&quot; alt=&quot;barracuda_install&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Wait for Unity to install the dependencies.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\barracuda_installation_progress.PNG&quot; alt=&quot;barracuda_installation_progress&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;import-posenet-model&quot;&gt;Import PoseNet Model&lt;/h2&gt;

&lt;p&gt;Now we can import the model into Unity. The Barracuda dev team has focused on supporting the &lt;a href=&quot;https://onnx.ai/&quot;&gt;ONNX&lt;/a&gt; format for models. We aren’t able to directly import models from TensorFlow or PyTorch. I’ve already converted the PoseNet model to ONNX. You can check out my tutorial for converting TensorFlow SavedModels to ONNX (&lt;a href=&quot;https://christianjmills.com/tensorflow/onnx/tutorial/2020/10/21/How-to-Convert-a-TensorFlow-SavedModel-to-ONNX.html&quot;&gt;here&lt;/a&gt;). PyTorch provides built-in support for ONNX (&lt;a href=&quot;https://pytorch.org/tutorials/advanced/super_resolution_with_onnxruntime.html&quot;&gt;link&lt;/a&gt;).&lt;/p&gt;

&lt;h3 id=&quot;download-the-onnx-file&quot;&gt;Download the ONNX File&lt;/h3&gt;

&lt;p&gt;You can download the converted PoseNet model from the link below.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;ResNet50: (&lt;a href=&quot;https://drive.google.com/file/d/1oKrlraI3m3ecme-pAvAh25-Jzzu86sv_/view?usp=sharing&quot;&gt;download&lt;/a&gt;)&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&quot;import-model-to-assets&quot;&gt;Import Model to Assets&lt;/h3&gt;

&lt;p&gt;Create a new folder in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Assets&lt;/code&gt; window and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;Models&lt;/code&gt;. Drag and drop the ONNX file into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Models&lt;/code&gt; folder.&lt;/p&gt;

&lt;p&gt;If you select the &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet50&lt;/code&gt; asset, you should see the following in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\resnet50_inspector_tab.PNG&quot; alt=&quot;resnet50_inspector_tab&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;load-the-model&quot;&gt;Load the Model&lt;/h2&gt;

&lt;p&gt;Next, we need to implement the code for loading the model in the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; &lt;a href=&quot;https://christianjmills.com/unity/tutorial/2020/11/04/Barracuda-PoseNet-Tutorial-2.html#create-the-posenet-script&quot;&gt;script&lt;/a&gt;.&lt;/p&gt;

&lt;h3 id=&quot;add-unitybarracuda-namespace&quot;&gt;Add &lt;code class=&quot;highlighter-rouge&quot;&gt;Unity.Barracuda&lt;/code&gt; Namespace&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script and add the &lt;code class=&quot;highlighter-rouge&quot;&gt;Unity.Barracuda&lt;/code&gt; namespace at the top of the script.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\add_barracuda_namespace.png&quot; alt=&quot;add_barracuda_namespace&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-modelasset-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;modelAsset&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Make a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;NNModel&lt;/code&gt; variable called &lt;code class=&quot;highlighter-rouge&quot;&gt;modelAsset&lt;/code&gt;. We’ll assign the &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet50&lt;/code&gt; asset to this variable in the Unity Editor.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_modelAsset_variable.png&quot; alt=&quot;create_modelAsset_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-workertype-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;workerType&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We’ll also add a variable that let’s us choose which &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/manual/Worker.html&quot;&gt;backend&lt;/a&gt; to use when performing inference. The options are divided into &lt;code class=&quot;highlighter-rouge&quot;&gt;CPU&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;GPU&lt;/code&gt;. I believe there are plans to add support for specialized hardware such as Neural Processing Units in the future. Our preprocessing pipeline runs entirely on the &lt;code class=&quot;highlighter-rouge&quot;&gt;GPU&lt;/code&gt; so we’ll be sticking with the &lt;code class=&quot;highlighter-rouge&quot;&gt;GPU&lt;/code&gt; options for this tutorial series.&lt;/p&gt;

&lt;p&gt;Make a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;WorkerFactory.Type&lt;/code&gt; called &lt;code class=&quot;highlighter-rouge&quot;&gt;workerType&lt;/code&gt;. Give it a default value of &lt;code class=&quot;highlighter-rouge&quot;&gt;WorkerFactory.Type.Auto&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_workerType_variable.png&quot; alt=&quot;load_model_variables_1&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-m_runtimemodel-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;m_RuntimeModel&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We need to compile the &lt;code class=&quot;highlighter-rouge&quot;&gt;modelAsset&lt;/code&gt; into a run-time model to perform inference. We’ll store the compiled model in a new private &lt;code class=&quot;highlighter-rouge&quot;&gt;Model&lt;/code&gt; variable called &lt;code class=&quot;highlighter-rouge&quot;&gt;m_RuntimeModel&lt;/code&gt;. This is the naming convention used in the Barracuda &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/manual/Loading.html&quot;&gt;documentation&lt;/a&gt;.&lt;/p&gt;

&lt;h3 id=&quot;create-engine-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;engine&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Next, we’ll create a new private &lt;code class=&quot;highlighter-rouge&quot;&gt;IWorker&lt;/code&gt; variable to store our inference engine. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;engine&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\load_model_variables_2.png&quot; alt=&quot;load_model_variables_2&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-heatmaplayer-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;heatmapLayer&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Add a new private &lt;code class=&quot;highlighter-rouge&quot;&gt;string&lt;/code&gt; variable to store the  name of the heatmap layer in the &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet50&lt;/code&gt; model. We’ll need the output of this layer to determine the location of key points (e.g. nose, elbows, knees, etc.) in the input image. We can find the name for the model’s output layers in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. For our model, the heatmap layer is named &lt;code class=&quot;highlighter-rouge&quot;&gt;float_heamap&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\resnet50_output_layers.PNG&quot; alt=&quot;resnet50_output_layers&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; The last two output layers, &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet_v1_50/displacement_bwd_2/BiasAd&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet_v1_50/displacement_fwd_2/BiasAd&lt;/code&gt;, are used when estimating the pose of multiple people. We’ll be sticking to single pose estimation for this series.&lt;/p&gt;

&lt;h3 id=&quot;create-offsetslayer-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;offsetsLayer&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We’ll go ahead and create a variable for the &lt;code class=&quot;highlighter-rouge&quot;&gt;float_short_offsets&lt;/code&gt; layer as well since we’ll need it later. The output from this layer is used to refine the estimated key point locations determined with the heatmap layer.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\layer_name_variables.png&quot; alt=&quot;layer_name_variables&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;compile-the-model&quot;&gt;Compile the Model&lt;/h3&gt;

&lt;p&gt;We need to get an object oriented representation of the model before we can work with it. We’ll do this in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method and store it in the &lt;code class=&quot;highlighter-rouge&quot;&gt;m_RuntimeModel&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\compile_model.png&quot; alt=&quot;compile_model&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;modify-the-model&quot;&gt;Modify the Model&lt;/h3&gt;

&lt;p&gt;We need to add a &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.ModelBuilder.html#Unity_Barracuda_ModelBuilder_Sigmoid_System_String_System_Object_&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Sigmoid&lt;/code&gt;&lt;/a&gt; layer to the end of the model before creating our inference engine. This will map the output values to the range &lt;code class=&quot;highlighter-rouge&quot;&gt;[0,1]&lt;/code&gt;. We’ll use these values to measure the model’s confidence that a given key point is in a given spot in the input image. A value of &lt;code class=&quot;highlighter-rouge&quot;&gt;1&lt;/code&gt; would indicate that the model is &lt;code class=&quot;highlighter-rouge&quot;&gt;100%&lt;/code&gt; confident the key point is in that location. We won’t be getting any &lt;code class=&quot;highlighter-rouge&quot;&gt;1&lt;/code&gt;’s.&lt;/p&gt;

&lt;p&gt;First, we need to make a new private &lt;code class=&quot;highlighter-rouge&quot;&gt;string&lt;/code&gt; variable to store the name of this new layer. We’ll name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;predictionLayer&lt;/code&gt; and name the layer &lt;code class=&quot;highlighter-rouge&quot;&gt;heatmap_predictions&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\predictionLayer_name.png&quot; alt=&quot;predictionLayer_name&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We’ll add the new layer using a &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.ModelBuilder.html&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;ModelBuilder&lt;/code&gt;&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\add_sigmoid_layer.png&quot; alt=&quot;add_sigmoid_layer&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;initialize-the-inference-engine&quot;&gt;Initialize the Inference Engine&lt;/h3&gt;

&lt;p&gt;Now we can create a worker to execute the modified model using the selected backend. We’ll do this using the &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.WorkerFactory.html#Unity_Barracuda_WorkerFactory_CreateWorker_Unity_Barracuda_WorkerFactory_Type_Unity_Barracuda_Model_System_Boolean_&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;WorkerFactory.CreateWorker()&lt;/code&gt;&lt;/a&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_worker.png&quot; alt=&quot;create_worker&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;release-inference-engine-resources&quot;&gt;Release Inference Engine Resources&lt;/h3&gt;

&lt;p&gt;We need to manually release the resources that get allocated for the inference &lt;code class=&quot;highlighter-rouge&quot;&gt;engine&lt;/code&gt;. This should be one of the last actions performed. Therefore, we’ll do it in the &lt;code class=&quot;highlighter-rouge&quot;&gt;OnDisable()&lt;/code&gt; method. This method gets called when the Unity project exits. We need to implement this method in the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\onDisable_method.png&quot; alt=&quot;onDisable_method&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;set-inspector-variables&quot;&gt;Set Inspector Variables&lt;/h2&gt;

&lt;p&gt;Now we just need to set the values for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Model Asset&lt;/code&gt; and select the inference backend.&lt;/p&gt;

&lt;h3 id=&quot;assign-the-model-asset&quot;&gt;Assign the Model Asset&lt;/h3&gt;

&lt;p&gt;With the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseEstimator&lt;/code&gt; object selected, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet50&lt;/code&gt; asset into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Model Asset&lt;/code&gt; variable.&lt;/p&gt;

&lt;h3 id=&quot;select-inference-backend&quot;&gt;Select Inference Backend&lt;/h3&gt;

&lt;p&gt;Set the backend to the &lt;code class=&quot;highlighter-rouge&quot;&gt;Compute Precompiled&lt;/code&gt; option in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Worker Type&lt;/code&gt; drop-down. This is the most efficient GPU backend.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\assign_model_asset_and_backend.PNG&quot; alt=&quot;assign_model_asset_and_backend&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;perform-inference&quot;&gt;Perform Inference&lt;/h2&gt;

&lt;p&gt;Finally, we’ll add the code to perform inference in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt; method.&lt;/p&gt;

&lt;h3 id=&quot;create-the-input-tensor&quot;&gt;Create the &lt;code class=&quot;highlighter-rouge&quot;&gt;input&lt;/code&gt; Tensor&lt;/h3&gt;

&lt;p&gt;We need to convert the &lt;code class=&quot;highlighter-rouge&quot;&gt;processedImage&lt;/code&gt; to a &lt;code class=&quot;highlighter-rouge&quot;&gt;Tensor&lt;/code&gt; before we can feed it to the model. The &lt;code class=&quot;highlighter-rouge&quot;&gt;Tensor&lt;/code&gt; constructor requires us to specify the number of channels in the image. We don’t need the alpha (transparency) channel so we’ll specify &lt;code class=&quot;highlighter-rouge&quot;&gt;3&lt;/code&gt; for the RGB color channels.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_input_tensor.png&quot; alt=&quot;create_input_tensor&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;execute-the-model&quot;&gt;Execute the Model&lt;/h3&gt;

&lt;p&gt;We’ll use the &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.IWorker.html#Unity_Barracuda_IWorker_Execute_Unity_Barracuda_Tensor_&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;engine.Execute()&lt;/code&gt;&lt;/a&gt; method to perform inference. This method takes in the input Tensor and schedules the network execution.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\execute_model.png&quot; alt=&quot;execute_model&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;release-input-tensor-resources&quot;&gt;Release Input Tensor Resources&lt;/h3&gt;

&lt;p&gt;We’ll need to manually release the allocated resources for the Tensor with the &lt;code class=&quot;highlighter-rouge&quot;&gt;input.Dispose()&lt;/code&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\dispose_input_tensor.png&quot; alt=&quot;dispose_input_tensor&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Here is the revised &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\perform_inference_update_method_2.png&quot; alt=&quot;perform_inference_update_method&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;

&lt;p&gt;We’ve finally performed inference using a PoseNet model in Unity. However, we need to process the output from the model to determine the estimated key point locations. We’ll cover how to perform the postprocessing operations in part 4.&lt;/p&gt;

&lt;h3 id=&quot;next-part-4&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-4/&quot;&gt;Part 4&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 25, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 2.5 (Optional)</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-2-5/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 2.5 (Optional)" /><published>2020-11-05T00:00:00-06:00</published><updated>2020-11-05T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-2-5</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-2-5/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 21, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-2&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-2/&quot;&gt;Part 2&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#make-a-new-screen&quot;&gt;Make a New Screen&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#update-the-posenet-script&quot;&gt;Update the PoseNet Script&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#assign-inspector-variables&quot;&gt;Assign Inspector Variables&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#test-the-new-screen&quot;&gt;Test the New Screen&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;make-a-new-screen&quot;&gt;Make a New Screen&lt;/h2&gt;

&lt;p&gt;If you want to see what the preprocessed images look like before they get fed into the model, we can make a second screen to view them during runtime.&lt;/p&gt;

&lt;h3 id=&quot;make-a-quad&quot;&gt;Make a Quad&lt;/h3&gt;

&lt;p&gt;In the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab, make an new Quad and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;InputScreen&lt;/code&gt;.&lt;/p&gt;

&lt;h4 id=&quot;set-the-scale&quot;&gt;Set the Scale&lt;/h4&gt;

&lt;p&gt;We’ll set both the &lt;code class=&quot;highlighter-rouge&quot;&gt;X&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;Y&lt;/code&gt; scale values to &lt;code class=&quot;highlighter-rouge&quot;&gt;360&lt;/code&gt; to match our current input resolution for the model.&lt;/p&gt;

&lt;h4 id=&quot;set-the-position&quot;&gt;Set the Position&lt;/h4&gt;

&lt;p&gt;We also need to set the &lt;code class=&quot;highlighter-rouge&quot;&gt;X&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;Y&lt;/code&gt; position values to &lt;code class=&quot;highlighter-rouge&quot;&gt;180&lt;/code&gt;. Set the &lt;code class=&quot;highlighter-rouge&quot;&gt;Z&lt;/code&gt; position value to &lt;code class=&quot;highlighter-rouge&quot;&gt;-1&lt;/code&gt; so that it’s in front of the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\set_inputScreen_transform.PNG&quot; alt=&quot;set_inputScreen_transform&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;make-a-rendertexture&quot;&gt;Make a RenderTexture&lt;/h3&gt;

&lt;p&gt;Make another &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderTexture&lt;/code&gt; and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;input_texture&lt;/code&gt;.&lt;/p&gt;

&lt;h4 id=&quot;set-the-size&quot;&gt;Set the Size&lt;/h4&gt;

&lt;p&gt;Set the &lt;code class=&quot;highlighter-rouge&quot;&gt;Size&lt;/code&gt; to &lt;code class=&quot;highlighter-rouge&quot;&gt;360 x 360&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\set_input_texture_size.PNG&quot; alt=&quot;set_input_texture_size&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;apply-the-input_texture&quot;&gt;Apply the &lt;code class=&quot;highlighter-rouge&quot;&gt;input_texture&lt;/code&gt;&lt;/h4&gt;

&lt;p&gt;Drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;input_texture&lt;/code&gt; onto the &lt;code class=&quot;highlighter-rouge&quot;&gt;InputScreen&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Scene&lt;/code&gt; tab.&lt;/p&gt;

&lt;h3 id=&quot;make-the-shader-unlit&quot;&gt;Make the Shader Unlit&lt;/h3&gt;

&lt;p&gt;Set the &lt;code class=&quot;highlighter-rouge&quot;&gt;Shader&lt;/code&gt; for the &lt;code class=&quot;highlighter-rouge&quot;&gt;InputScreen&lt;/code&gt; to &lt;code class=&quot;highlighter-rouge&quot;&gt;Unlit/Texture&lt;/code&gt; just like the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt;.&lt;/p&gt;

&lt;h2 id=&quot;update-the-posenet-script&quot;&gt;Update the PoseNet Script&lt;/h2&gt;

&lt;p&gt;Next, we need to create a few new public variables in the PoseNet script.&lt;/p&gt;

&lt;h3 id=&quot;add-displayinput-variable&quot;&gt;Add &lt;code class=&quot;highlighter-rouge&quot;&gt;displayInput&lt;/code&gt; variable&lt;/h3&gt;

&lt;p&gt;Create a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;bool&lt;/code&gt; variable called &lt;code class=&quot;highlighter-rouge&quot;&gt;displayInput&lt;/code&gt;. This will add a checkbox in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inpsector&lt;/code&gt; tab that we can use to turn the &lt;code class=&quot;highlighter-rouge&quot;&gt;InputScreen&lt;/code&gt; on and off.&lt;/p&gt;

&lt;h3 id=&quot;add-inputscreen-variable&quot;&gt;Add &lt;code class=&quot;highlighter-rouge&quot;&gt;inputScreen&lt;/code&gt; variable&lt;/h3&gt;

&lt;p&gt;Create a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; variable called &lt;code class=&quot;highlighter-rouge&quot;&gt;inputScreen&lt;/code&gt;. We need to access the &lt;code class=&quot;highlighter-rouge&quot;&gt;InputScreen&lt;/code&gt; object to activate and deactivate it.&lt;/p&gt;

&lt;h3 id=&quot;add-inputtexture-variable&quot;&gt;Add &lt;code class=&quot;highlighter-rouge&quot;&gt;inputTexture&lt;/code&gt; variable&lt;/h3&gt;

&lt;p&gt;Create a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderTexture&lt;/code&gt; variable called &lt;code class=&quot;highlighter-rouge&quot;&gt;inputTexture&lt;/code&gt;. We’ll assign the &lt;code class=&quot;highlighter-rouge&quot;&gt;input_texture&lt;/code&gt; asset to this variable in the Unity Editor.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\preview_preprocessed_input_variables.png&quot; alt=&quot;preview_preprocessed_input_variables&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;modify-the-update-method&quot;&gt;Modify the &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;We can use the &lt;code class=&quot;highlighter-rouge&quot;&gt;Graphics.Blit()&lt;/code&gt; method to copy the &lt;code class=&quot;highlighter-rouge&quot;&gt;processedImage&lt;/code&gt; data to the &lt;code class=&quot;highlighter-rouge&quot;&gt;inputTexture&lt;/code&gt; variable. We’ll use the &lt;code class=&quot;highlighter-rouge&quot;&gt;inputScreen.SetActive()&lt;/code&gt; method to activate and deactivate the &lt;code class=&quot;highlighter-rouge&quot;&gt;InputScreen&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\update_method_with_displayInput.png&quot; alt=&quot;update_method_with_displayInput&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;assign-inspector-variables&quot;&gt;Assign Inspector Variables&lt;/h2&gt;

&lt;p&gt;With the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseEstimator&lt;/code&gt; selected in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;InputScreen&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;input_texture&lt;/code&gt; into their respective variables in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\pose_estimator_displayInput.PNG&quot; alt=&quot;pose_estimator_displayInput&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;test-the-new-screen&quot;&gt;Test the New Screen&lt;/h2&gt;

&lt;p&gt;Make sure the &lt;code class=&quot;highlighter-rouge&quot;&gt;Display Input&lt;/code&gt; checkbox is ticked in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. It will be easier to see the changes to the preprocessed images if we use a full color video. We can set the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Clip&lt;/code&gt; for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; to the &lt;code class=&quot;highlighter-rouge&quot;&gt;pexels_woman_dancing&lt;/code&gt; file that we downloaded in &lt;a href=&quot;https://christianjmills.com/unity/tutorial/2020/10/25/Barracuda-PoseNet-Tutorial-1.html#import-video-files&quot;&gt;Part 1&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\preprocessed_image_preview6.gif&quot; alt=&quot;preprocessed_image_preview6&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;next-part-3&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-3/&quot;&gt;Part 3&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 21, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 2</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-2/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 2" /><published>2020-11-04T00:00:00-06:00</published><updated>2020-11-04T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-2</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-2/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 25, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-1&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-1/&quot;&gt;Part 1&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-a-compute-shader&quot;&gt;Create a Compute Shader&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-the-posenet-script&quot;&gt;Create the PoseNet Script&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-the-pose-estimator&quot;&gt;Create the Pose Estimator&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;The PoseNet model we’ll be using has a ResNet-50 architecture and was created using TensorFlow. It takes a single RGB image as input. We need to perform some preprocessing operations on the RGB channel values before feeding an image to the model. We’ll first scale the values so that they are in the same range that the model was trained on. We then subtract the mean RGB values for the ImageNet dataset.&lt;/p&gt;

&lt;h2 id=&quot;create-a-compute-shader&quot;&gt;Create a Compute Shader&lt;/h2&gt;

&lt;p&gt;We can perform the preprocessing steps more quickly on the GPU. In Unity, we accomplish this with &lt;a href=&quot;https://docs.unity3d.com/Manual/class-ComputeShader.html&quot;&gt;compute shaders&lt;/a&gt;. Compute shaders are pieces of code that can run parallel tasks on the graphics card. This is beneficial since we need to perform the same operations on every pixel in an image. It also frees up the CPU.&lt;/p&gt;

&lt;h3 id=&quot;create-the-asset-file&quot;&gt;Create the Asset File&lt;/h3&gt;

&lt;p&gt;Create a new folder in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Assets&lt;/code&gt; window and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;Shaders&lt;/code&gt;. Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;Shaders&lt;/code&gt; folder and right-click an empty space. Select &lt;code class=&quot;highlighter-rouge&quot;&gt;Shader&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Create&lt;/code&gt; submenu and click &lt;code class=&quot;highlighter-rouge&quot;&gt;Compute Shader&lt;/code&gt;. We’ll name it &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNetShader&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_compute_shader.PNG&quot; alt=&quot;create_compute_shader&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;remove-the-default-code&quot;&gt;Remove the Default Code&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNetShader&lt;/code&gt; in your code editor. By default, the &lt;code class=&quot;highlighter-rouge&quot;&gt;ComputeShader&lt;/code&gt; will contain the following.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\default_compute_shader.png&quot; alt=&quot;default_compute_shader&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Delete the &lt;code class=&quot;highlighter-rouge&quot;&gt;CSMain&lt;/code&gt; function along with the &lt;code class=&quot;highlighter-rouge&quot;&gt;#pragma kernel CSMain&lt;/code&gt;. Next, we need to add a &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt; variable to store the input image. Name it &lt;code class=&quot;highlighter-rouge&quot;&gt;InputImage&lt;/code&gt; and give it a data type of &lt;code class=&quot;highlighter-rouge&quot;&gt;&amp;lt;half4&amp;gt;&lt;/code&gt;. Use the same data type for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Result&lt;/code&gt; variable as well.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\posenet_shader_part1.png&quot; alt=&quot;posenet_shader_part1&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-preprocessresnet-function&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;PreprocessResNet&lt;/code&gt; Function&lt;/h3&gt;

&lt;p&gt;We need to make a new function to apply the ResNet preprocessing. Name the new function &lt;code class=&quot;highlighter-rouge&quot;&gt;PreprocessResNet()&lt;/code&gt;. We’ll use the default &lt;code class=&quot;highlighter-rouge&quot;&gt;[numthreads(8,8,1)]&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\posenet_shader_part2v3.png&quot; alt=&quot;posenet_shader_part2&quot; /&gt;&lt;/p&gt;

&lt;p&gt;The &lt;code class=&quot;highlighter-rouge&quot;&gt;PreprocessResNet&lt;/code&gt; function scales the RGB channel values of every pixel in the &lt;code class=&quot;highlighter-rouge&quot;&gt;InputImage&lt;/code&gt; by &lt;code class=&quot;highlighter-rouge&quot;&gt;255&lt;/code&gt;. By default, color values in Unity are in the range of &lt;code class=&quot;highlighter-rouge&quot;&gt;[0,1]&lt;/code&gt;. The function then substracts the ImageNet mean specific to the RGB channels. The processed image is returned in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Result&lt;/code&gt; variable.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th&gt;Channel&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;ImageNet Mean&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;Red&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;123.15&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;Green&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;115.90&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;Blue&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;103.06&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\posenet_shader_part3v5.png&quot; alt=&quot;posenet_compute_shader&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Now that we’ve created our &lt;code class=&quot;highlighter-rouge&quot;&gt;ComputeShader&lt;/code&gt;, we need to execute it using a &lt;code class=&quot;highlighter-rouge&quot;&gt;C#&lt;/code&gt; script.&lt;/p&gt;

&lt;h2 id=&quot;create-the-posenet-script&quot;&gt;Create the PoseNet Script&lt;/h2&gt;

&lt;p&gt;We need to make a new &lt;code class=&quot;highlighter-rouge&quot;&gt;C#&lt;/code&gt; script to perform inference with the PoseNet model. When finished, this script will load the model, prepare the input, run the model, and process the output. For this post, we’ll implement the preprocessing functionality.&lt;/p&gt;

&lt;h3 id=&quot;create-the-asset-file-1&quot;&gt;Create the Asset File&lt;/h3&gt;

&lt;p&gt;Create a new folder in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Assets&lt;/code&gt; window and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;Scripts&lt;/code&gt;. In the &lt;code class=&quot;highlighter-rouge&quot;&gt;Scripts&lt;/code&gt; folder, right-click an empty space and select &lt;code class=&quot;highlighter-rouge&quot;&gt;C# Script&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Create&lt;/code&gt; submenu.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_new_script.PNG&quot; alt=&quot;create_new_script&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Name the script &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\new_posenet_script.PNG&quot; alt=&quot;new_posenet_script&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Open the script in your code editor.&lt;/p&gt;

&lt;h3 id=&quot;create-videotexture-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Above the start method, create a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderTexture&lt;/code&gt; named &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;. This is the variable to which we’ll assign the &lt;code class=&quot;highlighter-rouge&quot;&gt;video_texture&lt;/code&gt; that we made in part 1.&lt;/p&gt;

&lt;h3 id=&quot;create-posenetshader-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;posenetShader&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We’ll also create a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;ComputeShader&lt;/code&gt; variable and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;posenetShader&lt;/code&gt;. We’ll assign the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNetShader&lt;/code&gt; to this variable in the Unity Editor.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_posenetShader_variable.png&quot; alt=&quot;create_videoTexture_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-preprocessimage-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;PreprocessImage()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;Next, we need to make a new method to handle the preprocessing steps for the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;. We’ll name this method &lt;code class=&quot;highlighter-rouge&quot;&gt;PreprocessImage&lt;/code&gt; and define it below the &lt;code class=&quot;highlighter-rouge&quot;&gt;Update&lt;/code&gt; method. The method will return a &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt; that contains the preprocessed image.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\preprocessImage_method_new.png&quot; alt=&quot;preprocessImage_method_new&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;create-a-new-texture2d&quot;&gt;Create a New Texture2D&lt;/h4&gt;

&lt;p&gt;We don’t want to alter the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; directly, so we’ll make a copy of the current frame. Create a new &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt; called &lt;code class=&quot;highlighter-rouge&quot;&gt;imageTexture&lt;/code&gt; and give it the same dimensions as the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;. We can use the &lt;code class=&quot;highlighter-rouge&quot;&gt;Graphics.CopyTexture()&lt;/code&gt; method to copy the data from the &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderTexture&lt;/code&gt; directly on the GPU.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\preprocessImage_method_create_imageTexture_2.png&quot; alt=&quot;preprocessImage_method_create_imageTexture&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;resize-the-image&quot;&gt;Resize the Image&lt;/h4&gt;

&lt;p&gt;Now that we have our &lt;code class=&quot;highlighter-rouge&quot;&gt;imageTexture&lt;/code&gt;, we need to resize it to a more practical resolution. Lowering the resolution does decrease the model’s accuracy. Unfortunately, using a higher resolution can significantly impact inference speed. We’ll examine this trade-off in a later post.&lt;/p&gt;

&lt;p&gt;For now, we’ll use a resolution of &lt;code class=&quot;highlighter-rouge&quot;&gt;360 x 360&lt;/code&gt;. Create two new public &lt;code class=&quot;highlighter-rouge&quot;&gt;int&lt;/code&gt; variables for the image height and width respectively. This will make it easier to experiment with different resolutions.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\image_height_and_width_variables.png&quot; alt=&quot;image_height_and_width_variables&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We’ll make a new method to handle the resizing process. The method will take in a &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt; as well as the new height and width. It will return a &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt; with the new resolution.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\resize_method.png&quot; alt=&quot;resize_method&quot; /&gt;&lt;/p&gt;

&lt;p&gt;The &lt;code class=&quot;highlighter-rouge&quot;&gt;Graphics.CopyTexture()&lt;/code&gt; method requires that the source and destination textures be the same size. That means we need to destroy the current &lt;code class=&quot;highlighter-rouge&quot;&gt;imageTexture&lt;/code&gt; and make a temporary one with the smaller dimensions.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\preprocessImage_method_resize_image_3.png&quot; alt=&quot;preprocessImage_method_resize_image&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; Resizing the image to &lt;code class=&quot;highlighter-rouge&quot;&gt;360 x 360&lt;/code&gt; will squish our input image from a 16:9 aspect ratio to a square aspect ratio. We’ll need to account for this when we get to the postprocessing section.&lt;/p&gt;

&lt;h4 id=&quot;apply-model-specific-preprocessing&quot;&gt;Apply Model-Specific Preprocessing&lt;/h4&gt;

&lt;p&gt;This is where we’ll make use of the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNetShader&lt;/code&gt; we made earlier. We’ll create a new method to handle the execution process. Name the new method &lt;code class=&quot;highlighter-rouge&quot;&gt;PreprocessResNet&lt;/code&gt; to match the function in the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNetShader&lt;/code&gt;. They don’t need to have the same name. It’s just personal preference.&lt;/p&gt;

&lt;p&gt;For this method, we need to use HDR texture formats for the &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderTexture&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt;. This allows us to feed images into the model with color values outside of the standard range of &lt;code class=&quot;highlighter-rouge&quot;&gt;[0,1]&lt;/code&gt;. The Barracuda library &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.Tensor.html#Unity_Barracuda_Tensor__ctor_UnityEngine_Texture_System_Int32_System_String_&quot;&gt;remaps&lt;/a&gt; non-HDR color values to &lt;code class=&quot;highlighter-rouge&quot;&gt;[0,1]&lt;/code&gt;. Given that we’re scaling the values by &lt;code class=&quot;highlighter-rouge&quot;&gt;255&lt;/code&gt;, this is undesirable.&lt;/p&gt;

&lt;p&gt;You can view the full &lt;code class=&quot;highlighter-rouge&quot;&gt;PreprocessResNet&lt;/code&gt; method below.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\preprocessResNet_method_9.png&quot; alt=&quot;preprocessResNet_method&quot; /&gt;&lt;/p&gt;

&lt;p&gt;The &lt;code class=&quot;highlighter-rouge&quot;&gt;PreprocessResNet&lt;/code&gt; method returns a Texture2D with an HDR texture format. The switch to HDR texture formats means the &lt;code class=&quot;highlighter-rouge&quot;&gt;tempTex&lt;/code&gt; variable is no longer compatible. Fortunately, we can reuse the &lt;code class=&quot;highlighter-rouge&quot;&gt;imageTexture&lt;/code&gt; variable that we emptied.&lt;/p&gt;

&lt;p&gt;The finished &lt;code class=&quot;highlighter-rouge&quot;&gt;PreprocessImage&lt;/code&gt; method looks like this.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\preprocessImage_method_complete_3.png&quot; alt=&quot;preprocessImage_method_complete&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;call-the-method&quot;&gt;Call the Method&lt;/h4&gt;

&lt;p&gt;We’ll call &lt;code class=&quot;highlighter-rouge&quot;&gt;PreprocessImage()&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt; method so that it runs every frame.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\call_preprocessImage_method.png&quot; alt=&quot;call_preprocessImage_method&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;create-the-pose-estimator&quot;&gt;Create the Pose Estimator&lt;/h2&gt;

&lt;p&gt;To run the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script, we need to attach it to a &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; in the Unity Editor.&lt;/p&gt;

&lt;h3 id=&quot;create-an-empty-gameobject&quot;&gt;Create an Empty &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;In the Hierarchy tab, right-click an empty space and select &lt;code class=&quot;highlighter-rouge&quot;&gt;Create Empty&lt;/code&gt; from the menu. Name the empty GameObject &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseEstimator&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_poseEstimator_gameobject.PNG&quot; alt=&quot;create_poseEstimator_gameobject&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;attach-the-posenet-script&quot;&gt;Attach the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; Script&lt;/h3&gt;

&lt;p&gt;With the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseEstimator&lt;/code&gt; object selected, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\posenet_script_inspector_empty.PNG&quot; alt=&quot;posenet_script_inspector_empty&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;assign-the-video_texture&quot;&gt;Assign the &lt;code class=&quot;highlighter-rouge&quot;&gt;video_texture&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;Next, we need to assign the &lt;code class=&quot;highlighter-rouge&quot;&gt;video_texture&lt;/code&gt; asset to the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Texture&lt;/code&gt; parameter. With the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseEstimator&lt;/code&gt; object selected, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;video_texture&lt;/code&gt; asset into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Texture&lt;/code&gt; spot in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;h3 id=&quot;assign-the-posenetshader&quot;&gt;Assign the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNetShader&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;We also need to drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNetShader&lt;/code&gt; asset into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Posenet Shader&lt;/code&gt; spot in the the Inspector tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\posenet_script_inspector_full_1.PNG&quot; alt=&quot;posenet_script_inspector_full_1&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;

&lt;p&gt;We’re now ready to feed video frames to our PoseNet model. In part 3, we’ll cover how to install the Barracuda library and perform inference with our model.&lt;/p&gt;

&lt;h3 id=&quot;next-part-25optional-part-3&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-2-5/&quot;&gt;Part 2.5(Optional)&lt;/a&gt; &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-3/&quot;&gt;Part 3&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 25, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 1</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-1/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 1" /><published>2020-10-25T00:00:00-05:00</published><updated>2020-10-25T00:00:00-05:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-1</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-1/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 24, 2020&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#prerequisites&quot;&gt;Prerequisites&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-a-new-project&quot;&gt;Create a New Project&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#import-video-files&quot;&gt;Import Video Files&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-the-video-player&quot;&gt;Create the Video Player&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-the-video-screen&quot;&gt;Create the Video Screen&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#test-the-video-player&quot;&gt;Test the Video Player&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;This tutorial series provides step-by-step instructions for how to perform human &lt;a href=&quot;https://www.fritz.ai/pose-estimation/&quot;&gt;pose estimation&lt;/a&gt; in &lt;a href=&quot;https://unity.com/&quot;&gt;Unity&lt;/a&gt; with the &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/manual/index.html&quot;&gt;Barracuda&lt;/a&gt; inference library. We’ll be using a pretrained &lt;a href=&quot;https://medium.com/tensorflow/real-time-human-pose-estimation-in-the-browser-with-tensorflow-js-7dd0bc881cd5&quot;&gt;PoseNet&lt;/a&gt; model to estimate the 2D locations of key points on an individual’s body.&lt;/p&gt;

&lt;p&gt;This post demonstrates how to play and view videos inside Unity. We’ll later perform pose estimation on individual frames while the video is playing. We can gauge the model’s accuracy by comparing the estimated key point locations to the source video.&lt;/p&gt;

&lt;h2 id=&quot;prerequisites&quot;&gt;Prerequisites&lt;/h2&gt;

&lt;p&gt;I recommend checking the following prerequisites if you want to follow along on your own computer.&lt;/p&gt;

&lt;h3 id=&quot;unity&quot;&gt;Unity&lt;/h3&gt;

&lt;p&gt;This tutorial assumes that you have Unity installed. You can get acquainted with Unity by clicking on one of the tutorials listed below.&lt;/p&gt;

&lt;h3 id=&quot;how-to-make-a-game---unity-beginner-tutorial&quot;&gt;How to Make a Game - Unity Beginner Tutorial&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://www.youtube.com/watch?v=OR0e-1UBEOU&quot;&gt;Unity 2019.3&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://www.youtube.com/watch?v=Lu76c85LhGY&quot;&gt;Unity 2020.1&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; You can download the exact version of Unity used for this tutorial by clicking the link below.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;unityhub://2019.4.13f1/518737b1de84&quot;&gt;Unity 2019.4.13&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&quot;hardware&quot;&gt;Hardware&lt;/h3&gt;

&lt;p&gt;We’ll be performing &lt;a href=&quot;https://www.intel.com/content/www/us/en/artificial-intelligence/posts/deep-learning-training-and-inference.html&quot;&gt;inference&lt;/a&gt; on the GPU for this series. If possible, use a graphics card from a recent generation.&lt;/p&gt;

&lt;h2 id=&quot;create-a-new-project&quot;&gt;Create a New Project&lt;/h2&gt;

&lt;p&gt;First, we need to create a new Unity project. We’ll select the 2D template since the PoseNet model only estimates 2D poses.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_project.PNG&quot; alt=&quot;create_project&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;import-video-files&quot;&gt;Import Video Files&lt;/h2&gt;

&lt;p&gt;We’ll be using these two videos available on &lt;a href=&quot;https://www.pexels.com/&quot;&gt;Pexels&lt;/a&gt;, a free stock photos &amp;amp; videos site. The first one is easier for the PoseNet model. The second has some more challenging sections. Download the videos in &lt;code class=&quot;highlighter-rouge&quot;&gt;Full HD&lt;/code&gt; resolution.&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;a href=&quot;https://www.pexels.com/video/two-young-men-doing-a-boardslide-over-a-railing-4824358/&quot;&gt;Two Young Men Doing a Boardslide Over a Railing&lt;/a&gt;&lt;/p&gt;

    &lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; Renamed to &lt;code class=&quot;highlighter-rouge&quot;&gt;pexels_boardslides&lt;/code&gt;&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;a href=&quot;https://www.pexels.com/video/woman-dancing-2873755/&quot;&gt;Woman Dancing&lt;/a&gt;&lt;/p&gt;

    &lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; Renamed to &lt;code class=&quot;highlighter-rouge&quot;&gt;pexels_woman_dancing&lt;/code&gt;&lt;/p&gt;
  &lt;/li&gt;
&lt;/ol&gt;

&lt;h3 id=&quot;create-the-videos-folder&quot;&gt;Create the Videos Folder&lt;/h3&gt;

&lt;p&gt;In the &lt;code class=&quot;highlighter-rouge&quot;&gt;Assets&lt;/code&gt; window, right-click an empty space, select the &lt;code class=&quot;highlighter-rouge&quot;&gt;Create&lt;/code&gt; option, and click &lt;code class=&quot;highlighter-rouge&quot;&gt;Folder&lt;/code&gt;. Name the folder &lt;code class=&quot;highlighter-rouge&quot;&gt;Videos&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_folder.PNG&quot; alt=&quot;create_folder&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Double-click the &lt;code class=&quot;highlighter-rouge&quot;&gt;Videos&lt;/code&gt; folder to open it.&lt;/p&gt;

&lt;h3 id=&quot;add-video-files&quot;&gt;Add Video Files&lt;/h3&gt;

&lt;p&gt;Drag and drop the two video files from the file explorer into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Videos&lt;/code&gt; folder.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\video_file_assets.PNG&quot; alt=&quot;video_file_assets&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;create-the-video-player&quot;&gt;Create the Video Player&lt;/h2&gt;

&lt;p&gt;In the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab, right-click an empty area, select the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video&lt;/code&gt; section, and click &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt;. This will create a new &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; called &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt;. The default name works well enough so we’ll leave it as is.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_video_player.PNG&quot; alt=&quot;create_video_player&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;set-video-clip&quot;&gt;Set Video Clip&lt;/h3&gt;

&lt;p&gt;Select the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; object in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab. Then, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;pexels_boardslides&lt;/code&gt; file into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Clip&lt;/code&gt; parameter in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\video_clip_filled.png&quot; alt=&quot;video_clip_filled&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;make-the-video-loop&quot;&gt;Make the Video Loop&lt;/h3&gt;

&lt;p&gt;Tick the &lt;code class=&quot;highlighter-rouge&quot;&gt;Loop&lt;/code&gt; checkbox in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab to make the video repeat when the project is running.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\loop_video_checkbox.png&quot; alt=&quot;loop_video_checkbox&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;create-the-video-screen&quot;&gt;Create the Video Screen&lt;/h2&gt;

&lt;p&gt;We need to make a “screen” in Unity to watch the video. We’ll use a &lt;a href=&quot;https://docs.unity3d.com/ScriptReference/RenderTexture.html&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Render Texture&lt;/code&gt;&lt;/a&gt; to store the data for the current frame and attach it to the surface of a &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt;.&lt;/p&gt;

&lt;h3 id=&quot;create-a-render-texture&quot;&gt;Create a Render Texture&lt;/h3&gt;

&lt;p&gt;Create a new folder in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Assets&lt;/code&gt; window and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;Textures&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\textures_folder.PNG&quot; alt=&quot;textures_folder&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Open the folder and right-click an empty space. Select &lt;code class=&quot;highlighter-rouge&quot;&gt;Render Texture&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Create&lt;/code&gt; submenu and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;video_texture&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_render_texture.PNG&quot; alt=&quot;create_render_texture&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;resize-the-render-texture&quot;&gt;Resize the Render Texture&lt;/h3&gt;

&lt;p&gt;Select the &lt;code class=&quot;highlighter-rouge&quot;&gt;video_texture&lt;/code&gt; asset and set the &lt;code class=&quot;highlighter-rouge&quot;&gt;Size&lt;/code&gt; to &lt;code class=&quot;highlighter-rouge&quot;&gt;1920 x 1080&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. This will match the &lt;code class=&quot;highlighter-rouge&quot;&gt;video_texture&lt;/code&gt; to the resolution of our videos.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\rt_set_resolution.png&quot; alt=&quot;rt_set_resolution&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;assign-the-render-texture&quot;&gt;Assign the Render Texture&lt;/h3&gt;

&lt;p&gt;With the resolution set, select the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; object in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab again. Drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;video_texture&lt;/code&gt; object into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Target Texture&lt;/code&gt; parameter in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\target_texture_filled.png&quot; alt=&quot;target_texture_filled&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-the-screen-gameobject&quot;&gt;Create the Screen GameObject&lt;/h3&gt;

&lt;p&gt;Now, we need to create the screen itself. We’ll use a &lt;a href=&quot;https://docs.unity3d.com/Manual/PrimitiveObjects.html&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Quad&lt;/code&gt;&lt;/a&gt; object for the screen. Right click an empty space in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab, select the &lt;code class=&quot;highlighter-rouge&quot;&gt;3D Object&lt;/code&gt; section and click &lt;code class=&quot;highlighter-rouge&quot;&gt;Quad&lt;/code&gt;. We can just name it &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\create_quad.PNG&quot; alt=&quot;create_quad&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;resize-the-screen&quot;&gt;Resize the Screen&lt;/h3&gt;

&lt;p&gt;With the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; object selected, we need to adjust the &lt;code class=&quot;highlighter-rouge&quot;&gt;Scale&lt;/code&gt; parameter in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. Set the &lt;code class=&quot;highlighter-rouge&quot;&gt;X&lt;/code&gt; value to 1920 and the &lt;code class=&quot;highlighter-rouge&quot;&gt;Y&lt;/code&gt; value to 1080. Leave the &lt;code class=&quot;highlighter-rouge&quot;&gt;Z&lt;/code&gt; value at 1.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\quad_scale_set.png&quot; alt=&quot;quad_scale_set&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;set-the-screen-position&quot;&gt;Set the Screen Position&lt;/h3&gt;

&lt;p&gt;Next, we’ll move &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; to make things easier when processing output from the model. We want the bottom left corner to be at the origin. Set the &lt;code class=&quot;highlighter-rouge&quot;&gt;X&lt;/code&gt; value for &lt;code class=&quot;highlighter-rouge&quot;&gt;Position&lt;/code&gt; to half the &lt;code class=&quot;highlighter-rouge&quot;&gt;X&lt;/code&gt; value for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Scale&lt;/code&gt; parameter. Do the same for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Y&lt;/code&gt; value. The new &lt;code class=&quot;highlighter-rouge&quot;&gt;Position&lt;/code&gt; values should be &lt;code class=&quot;highlighter-rouge&quot;&gt;X: 960 Y: 540 Z: 0&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\quad_position_set.png&quot; alt=&quot;quad_position_set&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;reset-the-scene-perspective&quot;&gt;Reset the Scene Perspective&lt;/h3&gt;

&lt;p&gt;We should center our perspective on the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt;. We can do so by selecting the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; object and pressing the &lt;code class=&quot;highlighter-rouge&quot;&gt;F&lt;/code&gt; key on our keyboard. You can zoom back in by scrolling up with your mouse wheel.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\recentered_video_screen.PNG&quot; alt=&quot;recentered_video_screen&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;apply-the-render-texture-to-the-screen&quot;&gt;Apply the Render Texture to the Screen&lt;/h3&gt;

&lt;p&gt;Drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;video_texture&lt;/code&gt; asset onto the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Scene&lt;/code&gt; tab. The &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; object should turn completely black.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\empty_screen.PNG&quot; alt=&quot;empty_screen&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;make-video-screen-unlit&quot;&gt;Make Video Screen Unlit&lt;/h3&gt;

&lt;p&gt;With the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; object selected, click the &lt;code class=&quot;highlighter-rouge&quot;&gt;Shader&lt;/code&gt; dropdown in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. Select the Unlit option and click &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture&lt;/code&gt;. This removes the need for a separate light source. The videos would look extremely dim with the &lt;code class=&quot;highlighter-rouge&quot;&gt;Standard&lt;/code&gt; shader.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\select_unlit_shader.PNG&quot; alt=&quot;select_unlit_shader&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\select_unlit_texture_shader.PNG&quot; alt=&quot;select_unlit_texture_shader&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;camera-setup&quot;&gt;Camera Setup&lt;/h2&gt;

&lt;p&gt;Before playing the video, we need to reposition and resize the &lt;code class=&quot;highlighter-rouge&quot;&gt;Main Camera&lt;/code&gt; object.&lt;/p&gt;

&lt;h3 id=&quot;set-camera-position&quot;&gt;Set Camera Position&lt;/h3&gt;

&lt;p&gt;Select the &lt;code class=&quot;highlighter-rouge&quot;&gt;Main Camera&lt;/code&gt; object in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab and set the &lt;code class=&quot;highlighter-rouge&quot;&gt;Position&lt;/code&gt; to same &lt;code class=&quot;highlighter-rouge&quot;&gt;X: 960 Y: 540&lt;/code&gt; as the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; object. Next, we need to set the &lt;code class=&quot;highlighter-rouge&quot;&gt;Z&lt;/code&gt; value for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Position&lt;/code&gt; to the opposite of the &lt;code class=&quot;highlighter-rouge&quot;&gt;X&lt;/code&gt; value.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\set_main_camera_position_new.png&quot; alt=&quot;set_main_camera_position&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;resize-the-camera&quot;&gt;Resize the Camera&lt;/h3&gt;

&lt;p&gt;Finally, we need to adjust the Size parameter to 540 in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\set_camera_size_new.png&quot; alt=&quot;set_camera_size_new&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;test-the-video-player&quot;&gt;Test the Video Player&lt;/h2&gt;

&lt;p&gt;Now we can finally click the play button and watch the video.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\play_button.png&quot; alt=&quot;play_button&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;result&quot;&gt;Result&lt;/h3&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\barracuda_posenet_tutorial_420p.gif&quot; alt=&quot;barracuda_posenet_tutorial_420p&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;

&lt;p&gt;We now have a video player that we can use to feed input to the PoseNet model. The next post covers how to prepare input for the model on the GPU.&lt;/p&gt;

&lt;h2 id=&quot;next-part-2&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-2/&quot;&gt;Part 2&lt;/a&gt;&lt;/h2&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 24, 2020</summary></entry><entry><title type="html">Getting Started</title><link href="https://christianjmills.com/2020/10/22/Getting-Started.html" rel="alternate" type="text/html" title="Getting Started" /><published>2020-10-22T00:00:00-05:00</published><updated>2020-10-22T00:00:00-05:00</updated><id>https://christianjmills.com/2020/10/22/Getting-Started</id><content type="html" xml:base="https://christianjmills.com/2020/10/22/Getting-Started.html">&lt;h2 id=&quot;topics&quot;&gt;Topics&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#deep-learning&quot;&gt;Deep Learning&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#unity&quot;&gt;Unity&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#blender&quot;&gt;Blender&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;deep-learning&quot;&gt;Deep Learning&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;a href=&quot;https://course.fast.ai/&quot;&gt;Fast.ai: Practical Deep Learning for Coders&lt;/a&gt;&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;Deeplearning.ai: Deep Learning Specialization&lt;/p&gt;

    &lt;ol&gt;
      &lt;li&gt;&lt;a href=&quot;https://www.youtube.com/playlist?list=PLkDaE6sCZn6Ec-XTbcX1uRg2_u4xOEky0&quot;&gt;Part 1: Neural Networks&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;https://www.youtube.com/playlist?list=PLkDaE6sCZn6Hn0vK8co82zjQtt3T2Nkqc&quot;&gt;Part 2: Tuning Networks&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;https://www.youtube.com/playlist?list=PLkDaE6sCZn6E7jZ9sN_xHwSHOdjUxUW_b&quot;&gt;Part 3: Structuring Machine Learning Projects&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;https://www.youtube.com/playlist?list=PLkDaE6sCZn6Gl29AoE31iwdVwSG-KnDzF&quot;&gt;Part 4: Convolutional Neural Networks&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;https://www.youtube.com/playlist?list=PLkDaE6sCZn6F6wUI9tvS_Gw1vaFAx6rd6&quot;&gt;Part 5: Sequence Models&lt;/a&gt;&lt;/li&gt;
    &lt;/ol&gt;
  &lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;unity&quot;&gt;Unity&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;a href=&quot;https://www.youtube.com/watch?v=OR0e-1UBEOU&amp;amp;list=PLB5_EOMkLx_VHKn4IISeNwhlDrb1948ZX&amp;amp;index=3&quot;&gt;How to Make a Game - Unity Beginner Tutorial&lt;/a&gt;&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;a href=&quot;https://learn.unity.com/course/create-with-code&quot;&gt;Create with Code&lt;/a&gt;&lt;/p&gt;
  &lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;blender&quot;&gt;Blender&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;
    &lt;h4 id=&quot;blender-beginner-tutorial-series&quot;&gt;&lt;a href=&quot;https://www.youtube.com/playlist?list=PLjEaoINr3zgEq0u2MzVgAaHEBt--xLB6U&quot;&gt;Blender Beginner Tutorial Series&lt;/a&gt;&lt;/h4&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;/li&gt;
&lt;/ol&gt;</content><author><name></name></author><summary type="html">Topics</summary></entry></feed>