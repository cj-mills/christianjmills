<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.0.0">Jekyll</generator><link href="https://christianjmills.com/feed.xml" rel="self" type="application/atom+xml" /><link href="https://christianjmills.com/" rel="alternate" type="text/html" /><updated>2020-12-18T14:28:36-06:00</updated><id>https://christianjmills.com/feed.xml</id><title type="html">Christian Mills</title><subtitle>My personal blog</subtitle><entry><title type="html">In-Game Style Transfer Experiments Pt.1</title><link href="https://christianjmills.com/In-Game-Style-Transfer-Experiments-1/" rel="alternate" type="text/html" title="In-Game Style Transfer Experiments Pt.1" /><published>2020-12-17T00:00:00-06:00</published><updated>2020-12-17T00:00:00-06:00</updated><id>https://christianjmills.com/In-Game-Style-Transfer-Experiments-1</id><content type="html" xml:base="https://christianjmills.com/In-Game-Style-Transfer-Experiments-1/">&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#kinematica-image-dataset&quot;&gt;Kinematica Image Dataset&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#video-stylization-model&quot;&gt;Video Stylization Model&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#options-for-optimization&quot;&gt;Options for Optimization&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#unitys-implementation-first-impressions&quot;&gt;Unity’s Implementation: First Impressions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;I spent a bit of time this week messing around with different style transfer experiments. I wanted to see if training the &lt;a href=&quot;https://github.com/pytorch/examples/tree/master/fast_neural_style&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;fast_neural_style&lt;/code&gt;&lt;/a&gt; model on images from the Kinematica demo would improve the output quality. I also got the model from the &lt;a href=&quot;https://github.com/OndrejTexler/Few-Shot-Patch-Based-Training&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Interactive Video Stylization Using Few-Shot Patch-Based Training&lt;/code&gt;&lt;/a&gt; project working in Unity. It was actually super easy, barely an inconvenience. Although, I can’t say the same for training the model. Finally, I started exploring Unity’s style transfer &lt;a href=&quot;https://github.com/UnityLabs/barracuda-style-transfer&quot;&gt;project&lt;/a&gt;. Calling my implementation basic might have been an understatement.&lt;/p&gt;

&lt;h2 id=&quot;kinematica-image-dataset&quot;&gt;Kinematica Image Dataset&lt;/h2&gt;

&lt;p&gt;I used &lt;a href=&quot;https://obsproject.com/&quot;&gt;OBS&lt;/a&gt; to record an approximately 13 minute video of the character running around the Kinematica demo. I then split the video into separate frames using &lt;a href=&quot;https://ffmpeg.org/&quot;&gt;ffmpeg&lt;/a&gt;. I also created mirrored copies of the frames to double my dataset. I ended up resizing the images to &lt;code class=&quot;highlighter-rouge&quot;&gt;640 x 480&lt;/code&gt; to speed up training. My experiment resulted in noticeably less flickering when running the demo. Although, I still find the flickering in my results a bit distracting. There are also some slight changes in color between the two models. However, the differences were mostly lost when creating the Gifs below.&lt;/p&gt;

&lt;h3 id=&quot;baseline&quot;&gt;Baseline&lt;/h3&gt;

&lt;p&gt;&lt;img src=&quot;..\images\in-game-style-transfer-experiments\part-1\base_mosaic_3.gif&quot; alt=&quot;base_mosaic_3&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;my-results&quot;&gt;My Results&lt;/h3&gt;

&lt;p&gt;&lt;img src=&quot;..\images\in-game-style-transfer-experiments\part-1\my_mosaic.gif&quot; alt=&quot;my_mosaic&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;video-stylization-model&quot;&gt;Video Stylization Model&lt;/h2&gt;

&lt;p&gt;Training this style transfer model is a bit more involved than the one I’ve been using so far. First, this model doesn’t learn from a source style image like the one below.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\in-game-style-transfer-experiments\part-1\mosaic.jpg&quot; alt=&quot;mosaic&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Instead, you need to provide a few stylized examples of images from your training dataset. I just used the style transfer model I’ve been working with to generate these examples. However, you need to put in a bit more work to get the best results. You need to create image masks for each image in the dataset like the one below.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\in-game-style-transfer-experiments\part-1\111_mask.png&quot; alt=&quot;111_mask&quot; /&gt;&lt;/p&gt;

&lt;p&gt;You also need to generate noise for these masks like the example below.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\in-game-style-transfer-experiments\part-1\111_noise.png&quot; alt=&quot;111_noise&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;training-results&quot;&gt;Training Results&lt;/h3&gt;

&lt;p&gt;I wanted to see how the model ran in Unity before investing the time to make the masks and noise, so I used one of the sample training datasets provided with the project. As you can see below, the model produces much less flickering than the previous model.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\in-game-style-transfer-experiments\part-1\lynx_380p_cropped.gif&quot; alt=&quot;lynx_380p_cropped&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;unity-performance&quot;&gt;Unity Performance&lt;/h3&gt;

&lt;p&gt;The model did a surprisingly okay job stylizing the Kinematica demo despite having only trained on one hundred images of a lynx. Flickering is also significantly reduced. It didn’t even give me any headaches importing the ONNX file into Unity. The only catch was performance. The model that I’ve been using runs &lt;code class=&quot;highlighter-rouge&quot;&gt;720 x 540&lt;/code&gt; at approximately 25fps. This new model runs the same resolution at about 9fps. You’d probably need to wait a couple generations of GPUs before you could get playable frame rates with the model as is.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\in-game-style-transfer-experiments\part-1\few_shot_mosaic.gif&quot; alt=&quot;few_shot_mosaic&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;options-for-optimization&quot;&gt;Options for Optimization&lt;/h2&gt;

&lt;h2 id=&quot;unitys-implementation-first-impressions&quot;&gt;Unity’s Implementation: First Impressions&lt;/h2&gt;

&lt;p&gt;In short, I have some homework to do.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\in-game-style-transfer-experiments\part-1\unity_style_transfer.gif&quot; alt=&quot;unity_style_transfer&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\in-game-style-transfer-experiments\part-1\unity_style_transfer_scene.png&quot; alt=&quot;unity_style_transfer_scene&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;</content><author><name></name></author><summary type="html">Introduction Kinematica Image Dataset Video Stylization Model Options for Optimization Unity’s Implementation: First Impressions Summary</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 9</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-9/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 9" /><published>2020-12-11T00:00:00-06:00</published><updated>2020-12-11T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-9</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-9/">&lt;h3 id=&quot;previous-part-8&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-8/&quot;&gt;Part 8&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#input-resolutions&quot;&gt;Input Resolutions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;Different applications can require different tradeoffs between speed and accuracy. The size of the heatmaps generated by the PoseNet model is the greatest factor for balancing this tradeoff. The heatmap resolution indicates how many distinct locations the model needs to make a prediction for in the input image. The fewer distinct locations, the larger and more precise the offset values need to be. It’s easier to estimate whether a key point is in a specific spot rather than how far a way it is from that spot. In this post, we’ll explore how adjusting the input resolution impacts both inference speed and prediction accuracy.&lt;/p&gt;

&lt;h2 id=&quot;input-resolutions&quot;&gt;Input Resolutions&lt;/h2&gt;

&lt;p&gt;The easiest way to change the heatmap size is to adjust the resolution for the input image. We’ll start off with an extremely low resolution and go up from there. The minimum confidence for each test is set to 80%.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; The GIFs were all recorded at 24fps so differences in frame rate will not be visible.&lt;/p&gt;

&lt;h3 id=&quot;160-x-160-at-111fps&quot;&gt;160 x 160 at 111fps&lt;/h3&gt;

&lt;p&gt;This is about as low a resolution we can go before the model completely falls apart. The output is barely useable even with ideal video. The model is rarely confident about it’s predictions as shown below.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\barracuda-posenet-tutorial\part-9\skateboards_160p.gif&quot; alt=&quot;skateboards_160p&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;320-x-320-at-100fps&quot;&gt;320 x 320 at 100fps&lt;/h3&gt;

&lt;p&gt;Moving up to &lt;code class=&quot;highlighter-rouge&quot;&gt;320p&lt;/code&gt; improves model accuracy dramatically while having a small impact on performance. The model is much more confident in it’s predictions. It does a solid job with the body key points. However, predictions for the nose, eyes, and ears are still not reliable.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\barracuda-posenet-tutorial\part-9\skateboards_320p.gif&quot; alt=&quot;skateboards_320p&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;480-x-480-at-85fps&quot;&gt;480 x 480 at 85fps&lt;/h3&gt;

&lt;p&gt;This is probably the sweet spot for balancing performance and accuracy. The model’s predictions are stable overall and inference speed is still fairly high.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\barracuda-posenet-tutorial\part-9\skateboards_480p.gif&quot; alt=&quot;skateboards_480p&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;640-x-640-at-59fps&quot;&gt;640 x 640 at 59fps&lt;/h3&gt;

&lt;p&gt;Here we start to dip below 60fps. The model’s predictions are more stable, but the difference is less noticable than previous jumps in resolutions.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\barracuda-posenet-tutorial\part-9\skateboards_640p.gif&quot; alt=&quot;skateboards_640p&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;840-x-840-at--35fps&quot;&gt;840 x 840 at  35fps&lt;/h3&gt;

&lt;p&gt;At this resolution, we see a significant impact on inference speed for little to no gain in accuracy. Different video content might benefit more from this high of a resolution. However, The performance trade off is likely not worth it.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\barracuda-posenet-tutorial\part-9\skateboards_800p.gif&quot; alt=&quot;skateboards_800p&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;

&lt;p&gt;That was a brief overview of how the PoseNet model performs at different resolutions. Model accuracy will vary depending on the video content and the location of the individual in the camera frame. However, the above demos should provide a decent predictor of model performance.&lt;/p&gt;</content><author><name></name></author><summary type="html">Previous: Part 8</summary></entry><entry><title type="html">Basic In-Game Style Transfer Tutorial</title><link href="https://christianjmills.com/Basic-In-Game-Style-Transfer-Tutorial/" rel="alternate" type="text/html" title="Basic In-Game Style Transfer Tutorial" /><published>2020-12-08T00:00:00-06:00</published><updated>2020-12-08T00:00:00-06:00</updated><id>https://christianjmills.com/Basic-In-Game-Style-Transfer-Tutorial</id><content type="html" xml:base="https://christianjmills.com/Basic-In-Game-Style-Transfer-Tutorial/">&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#select-a-unity-project&quot;&gt;Select a Unity Project&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#install-barracuda-package&quot;&gt;Install Barracuda Package&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-style-transfer-folder&quot;&gt;Create Style Transfer Folder&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#import-models&quot;&gt;Import Models&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#prepare-render-textures&quot;&gt;Prepare Render Textures&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-a-compute-shader&quot;&gt;Create a Compute Shader&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-styletransfer-script&quot;&gt;Create StyleTransfer Script&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;Unity has finally released the in-game style transfer project they’ve been &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/manual/images/BarracudaLanding.png&quot;&gt;teasing&lt;/a&gt; in the Barracuda &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/manual/index.html&quot;&gt;documentation&lt;/a&gt;. Their implementation is slightly more polished than my early &lt;a href=&quot;https://christianjmills.com/unity/style_transfer/2020/10/19/In-Game-Style-Transfer.html&quot;&gt;attempts&lt;/a&gt;. And by slightly, I mean they seem to have addressed every major complaint I had with my implementation. Be sure to check out their sample &lt;a href=&quot;https://github.com/UnityLabs/barracuda-style-transfer&quot;&gt;project&lt;/a&gt; as well as the accompanying blog &lt;a href=&quot;https://blogs.unity3d.com/2020/11/25/real-time-style-transfer-in-unity-using-deep-neural-networks/&quot;&gt;post&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;It’s exciting that Unity has started releasing projects that explore alternative uses for the Barracuda library. Hopefully, they’ll explore other deep learning applications in future projects. I would love to see projects that use GANs for dynamically generating in-game content.&lt;/p&gt;

&lt;p&gt;I plan to work on a more sophisticated implementation for in-game style transfer in the future, perhaps using some tricks from Unity’s implementation. However, I wanted to start with a basic implementation to serve as a baseline.&lt;/p&gt;

&lt;p&gt;This tutorial will cover how to use trained models from the &lt;a href=&quot;https://github.com/pytorch/examples/tree/master/fast_neural_style&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;fast_neural_style&lt;/code&gt;&lt;/a&gt; project provided by PyTorch. The models take in regular images and return stylized versions. We’ll get our input images from the in-game camera and display the stylized output to the user.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Important:&lt;/strong&gt; This is meant as a simple proof of concept and requires a powerful GPU to get playable frame rates. An RTX 20-series equivalent or newer is recommended.&lt;/p&gt;

&lt;h2 id=&quot;select-a-unity-project&quot;&gt;Select a Unity Project&lt;/h2&gt;

&lt;p&gt;I’ll be using the &lt;a href=&quot;https://github.com/Unity-Technologies/Kinematica_Demo/&quot;&gt;Kinematica_Demo&lt;/a&gt; project provided by Unity for this tutorial. It provides a great character model for testing different styles. However, feel free to follow along with a different project. This one is a bit large and takes a while to open the first time.&lt;/p&gt;

&lt;h3 id=&quot;download-kinematica-demo&quot;&gt;Download Kinematica Demo&lt;/h3&gt;

&lt;p&gt;You can download the Unity project by clicking on the link below. The zipped folder is approximately 1.2 GB.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;Kinematica_Demo_0.8.0-preview: (&lt;a href=&quot;https://github.com/Unity-Technologies/Kinematica_Demo/releases/download/0.8.0-preview/Kinematica_Demo_0.8.0-preview.zip&quot;&gt;download&lt;/a&gt;)&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&quot;add-project-to-unity-hub&quot;&gt;Add Project to Unity Hub&lt;/h3&gt;

&lt;p&gt;Once downloaded, unzip the folder and add the project to Unity Hub using the &lt;code class=&quot;highlighter-rouge&quot;&gt;Add&lt;/code&gt; button.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\unity_hub_add_project.png&quot; alt=&quot;unity_hub_add_project&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;set-the-unity-version&quot;&gt;Set the Unity Version&lt;/h3&gt;

&lt;p&gt;Select a Unity version from the drop-down menu. The demo project was made using Unity &lt;code class=&quot;highlighter-rouge&quot;&gt;2019.4.5f1&lt;/code&gt;. You can use a later &lt;code class=&quot;highlighter-rouge&quot;&gt;2019.4&lt;/code&gt; release if you don’t have that version installed.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;Unity 2019.4.13: (&lt;a href=&quot;unityhub://2019.4.13f1/518737b1de84&quot;&gt;download&lt;/a&gt;)&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\set-unity-version.png&quot; alt=&quot;set-unity-version-0-0&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;open-the-project&quot;&gt;Open the Project&lt;/h3&gt;

&lt;p&gt;Now we can open the project. We’ll be prompted to upgrade the project to the selected Unity version. Click &lt;code class=&quot;highlighter-rouge&quot;&gt;Confirm&lt;/code&gt; in the popup to upgrade the project. As mentioned earlier, this project takes a while to load the first time.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\upgrade-unity-version.png&quot; alt=&quot;set-unity-version&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;install-barracuda-package&quot;&gt;Install Barracuda Package&lt;/h2&gt;

&lt;p&gt;We’ll install the Barracuda package once the project has finished loading. Select the Package Manager tab in the Unity editor and type Barracuda into the search box.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\barracuda_search.PNG&quot; alt=&quot;barracuda_search&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Click the &lt;code class=&quot;highlighter-rouge&quot;&gt;Install&lt;/code&gt; button to install the package.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\barracuda_install.PNG&quot; alt=&quot;barracuda_install&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;create-style-transfer-folder&quot;&gt;Create Style Transfer Folder&lt;/h2&gt;

&lt;p&gt;We’ll place all our additions to the project in a new asset folder called &lt;code class=&quot;highlighter-rouge&quot;&gt;Style_Transfer&lt;/code&gt;. This will help keep things organized.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\style_transfer_folder.png&quot; alt=&quot;style_transfer_folder&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;import-models&quot;&gt;Import Models&lt;/h2&gt;

&lt;p&gt;Next, we need to add some style transfer models. PyTorch models need to be exported to the &lt;a href=&quot;https://onnx.ai/&quot;&gt;ONNX&lt;/a&gt; format before being imported to Unity. Fortunately, PyTorch provides built-in support for exporting to ONNX (&lt;a href=&quot;https://pytorch.org/tutorials/advanced/super_resolution_with_onnxruntime.html&quot;&gt;tutorial&lt;/a&gt;).&lt;/p&gt;

&lt;h3 id=&quot;download-onnx-files&quot;&gt;Download ONNX Files&lt;/h3&gt;

&lt;p&gt;You can download some exported style transfer models from the links below.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;
    &lt;p&gt;Mosaic: (&lt;a href=&quot;https://drive.google.com/file/d/1gnWUCTkLmDyUFHzMl7fk9F64vSoZk5jK/view?usp=sharing&quot;&gt;download&lt;/a&gt;)&lt;/p&gt;

    &lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\mosaic.jpg&quot; alt=&quot;mosaic&quot; /&gt;&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;Van Gogh Starry Night: (&lt;a href=&quot;https://drive.google.com/file/d/1vL5-NZo0Dn0ijkX5u94WoP_WWnxFIU3o/view?usp=sharing&quot;&gt;download&lt;/a&gt;)&lt;/p&gt;
  &lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\van-gogh-starry-night-google-art-project.jpg&quot; alt=&quot;van-gogh-starry-night-google-art-project&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;import-onnx-files-to-assets&quot;&gt;Import ONNX Files to Assets&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;Style_Transfer&lt;/code&gt; folder and make a new folder called &lt;code class=&quot;highlighter-rouge&quot;&gt;Models&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\create-models-folder.png&quot; alt=&quot;create-models-folder&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Drag and drop the ONNX files into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Models&lt;/code&gt; folder.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\imported_onnx_files.png&quot; alt=&quot;imported_onnx_files&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;prepare-render-textures&quot;&gt;Prepare Render Textures&lt;/h2&gt;

&lt;p&gt;Our basic process will involve taking the current frame from the in-game camera, feeding it to the model, getting the output, and displaying the processed output to the user. We’ll store the current camera frame and processed output in separate &lt;a href=&quot;https://docs.unity3d.com/ScriptReference/RenderTexture.html&quot;&gt;render textures&lt;/a&gt;.&lt;/p&gt;

&lt;h3 id=&quot;create-textures-folder&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;Textures&lt;/code&gt; Folder&lt;/h3&gt;

&lt;p&gt;Add a new folder called &lt;code class=&quot;highlighter-rouge&quot;&gt;Textures&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Style_Transfer&lt;/code&gt; folder.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\textures_folder.png&quot; alt=&quot;textures_folder&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create--asset-files&quot;&gt;Create  Asset Files&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;Textures&lt;/code&gt; folder and create two new &lt;code class=&quot;highlighter-rouge&quot;&gt;Render Texture&lt;/code&gt; assets.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\create_renderTexture.png&quot; alt=&quot;create_renderTexture&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Name the new assets &lt;code class=&quot;highlighter-rouge&quot;&gt;CameraInput&lt;/code&gt;, &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessedOutput&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\new_renderTexture_assets_2.png&quot; alt=&quot;new_renderTexture_assets&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;update-size-parameters&quot;&gt;Update Size Parameters&lt;/h3&gt;

&lt;p&gt;We need to use a fairly low resolution to get playable frame rates. Click an empty space in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Textures&lt;/code&gt; folder and press &lt;code class=&quot;highlighter-rouge&quot;&gt;Ctrl-a&lt;/code&gt; to select both render textures. Set the size the parameter to &lt;code class=&quot;highlighter-rouge&quot;&gt;720 x 540&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. Feel free to try higher resolutions if you happen to have an RTX 30-series or equivalent GPU.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\set_renderTexture_sizes_2.png&quot; alt=&quot;set_renderTexture_sizes&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;create-a-compute-shader&quot;&gt;Create a Compute Shader&lt;/h2&gt;

&lt;p&gt;We can perform both the preprocessing and postprocessing operations on the GPU since both the input and output are images. We’ll implement these steps in a &lt;a href=&quot;https://docs.unity3d.com/Manual/class-ComputeShader.html&quot;&gt;compute shader&lt;/a&gt;.&lt;/p&gt;

&lt;h3 id=&quot;create-the-asset-file&quot;&gt;Create the Asset File&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;Style_Transer&lt;/code&gt; folder and create a new folder called &lt;code class=&quot;highlighter-rouge&quot;&gt;Shaders&lt;/code&gt;. Enter the &lt;code class=&quot;highlighter-rouge&quot;&gt;Shaders&lt;/code&gt; folder and right-click an empty space. Select &lt;code class=&quot;highlighter-rouge&quot;&gt;Shader&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Create&lt;/code&gt; submenu and click &lt;code class=&quot;highlighter-rouge&quot;&gt;Compute Shader&lt;/code&gt;. We’ll name it &lt;code class=&quot;highlighter-rouge&quot;&gt;StyleTransferShader&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\create-compute-shader.png&quot; alt=&quot;create-compute-shader&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;remove-the-default-code&quot;&gt;Remove the Default Code&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNetShader&lt;/code&gt; in your code editor. By default, the &lt;code class=&quot;highlighter-rouge&quot;&gt;ComputeShader&lt;/code&gt; will contain the following.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\default_compute_shader.png&quot; alt=&quot;default_compute_shader&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Delete the &lt;code class=&quot;highlighter-rouge&quot;&gt;CSMain&lt;/code&gt; function along with the &lt;code class=&quot;highlighter-rouge&quot;&gt;#pragma kernel CSMain&lt;/code&gt;. Next, we need to add a &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt; variable to store the input image. Name it &lt;code class=&quot;highlighter-rouge&quot;&gt;InputImage&lt;/code&gt; and give it a data type of &lt;code class=&quot;highlighter-rouge&quot;&gt;&amp;lt;half4&amp;gt;&lt;/code&gt;. Use the same data type for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Result&lt;/code&gt; variable as well.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\styleTransfer_shader_part1.png&quot; alt=&quot;styleTransfer_shader_part1&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-processinput-function&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessInput&lt;/code&gt; Function&lt;/h3&gt;

&lt;p&gt;The style transfer models expect RGB channel values to be in range &lt;code class=&quot;highlighter-rouge&quot;&gt;[0, 255]&lt;/code&gt;. Color values in Unity are in the range &lt;code class=&quot;highlighter-rouge&quot;&gt;[0,1]&lt;/code&gt;. Therefore, we need to scale the three channel values for the &lt;code class=&quot;highlighter-rouge&quot;&gt;InputImage&lt;/code&gt; by &lt;code class=&quot;highlighter-rouge&quot;&gt;255&lt;/code&gt;. We’ll perform this step in a new function called &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessInput&lt;/code&gt; as shown below.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\processInput_compute_shader.png&quot; alt=&quot;processInput_compute_shader&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-processoutput-function&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput&lt;/code&gt; Function&lt;/h3&gt;

&lt;p&gt;The models are supposed to output an image with RGB channel values in the range &lt;code class=&quot;highlighter-rouge&quot;&gt;[0, 255]&lt;/code&gt;. However, it can sometimes return values a little outside that range. We can use the built-in &lt;a href=&quot;https://docs.microsoft.com/en-us/windows/win32/direct3dhlsl/dx-graphics-hlsl-clamp&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;clamp()&lt;/code&gt;&lt;/a&gt; method to make sure all values are in the correct range. We’ll then scale the values back down to &lt;code class=&quot;highlighter-rouge&quot;&gt;[0, 1]&lt;/code&gt; for Unity. We’ll perform these steps in a new function called &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput&lt;/code&gt; as shown below.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\processOutput_compute_shader.png&quot; alt=&quot;processOutput_compute_shader&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Now that we’ve created our &lt;code class=&quot;highlighter-rouge&quot;&gt;ComputeShader&lt;/code&gt;, we need to execute it using a &lt;code class=&quot;highlighter-rouge&quot;&gt;C#&lt;/code&gt; script.&lt;/p&gt;

&lt;h2 id=&quot;create-styletransfer-script&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;StyleTransfer&lt;/code&gt; Script&lt;/h2&gt;

&lt;p&gt;We need to make a new &lt;code class=&quot;highlighter-rouge&quot;&gt;C#&lt;/code&gt; script to perform inference with the style transfer models. This script will load the model, process the input, runt the model, and process the output.&lt;/p&gt;

&lt;h3 id=&quot;create-the-asset-file-1&quot;&gt;Create the Asset File&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;Style_Transfer&lt;/code&gt; folder and create a new folder called &lt;code class=&quot;highlighter-rouge&quot;&gt;Scripts&lt;/code&gt;. In the &lt;code class=&quot;highlighter-rouge&quot;&gt;Scripts&lt;/code&gt; folder, right-click an empty space and select &lt;code class=&quot;highlighter-rouge&quot;&gt;C# Script&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Create&lt;/code&gt; submenu.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\create_c_sharp_script.png&quot; alt=&quot;create_c_sharp_script&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Name the script &lt;code class=&quot;highlighter-rouge&quot;&gt;StyleTransfer&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\styleTransfer_script_new.png&quot; alt=&quot;styleTransfer_script_new&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;add-unitybarracuda-namespace&quot;&gt;Add &lt;code class=&quot;highlighter-rouge&quot;&gt;Unity.Barracuda&lt;/code&gt; Namespace&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;StyleTransfer&lt;/code&gt; script and add the &lt;code class=&quot;highlighter-rouge&quot;&gt;Unity.Barracuda&lt;/code&gt; namespace at the top of the script.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\add_barracuda_namespace.png&quot; alt=&quot;add_barracuda_namespace&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-rendertexture-variables&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderTexture&lt;/code&gt; Variables&lt;/h3&gt;

&lt;p&gt;We need to create some public variables that we can use to access our two render texture assets in the script.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\renderTexture_variables_2.png&quot; alt=&quot;renderTexture_variables&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-styletransfershader-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;StyleTransferShader&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Next, we’ll add a public variable to access our compute shader.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\styleTransferShader_variable_2.png&quot; alt=&quot;styleTransferShader_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-barracuda-variables&quot;&gt;Create Barracuda Variables&lt;/h3&gt;

&lt;p&gt;Now we need to add a few variables to perform inference with the style transfer models.&lt;/p&gt;

&lt;h4 id=&quot;create-modelasset-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;modelAsset&lt;/code&gt; Variable&lt;/h4&gt;

&lt;p&gt;Make a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;NNModel&lt;/code&gt; variable called &lt;code class=&quot;highlighter-rouge&quot;&gt;modelAsset&lt;/code&gt;. We’ll assign one of the ONNX files to this variable in the Unity Editor.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\modelAsset_variable_2.png&quot; alt=&quot;modelAsset_variable&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;create-workertype-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;workerType&lt;/code&gt; Variable&lt;/h4&gt;

&lt;p&gt;We’ll also add a variable that let’s us choose which &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/manual/Worker.html&quot;&gt;backend&lt;/a&gt; to use when performing inference. The options are divided into &lt;code class=&quot;highlighter-rouge&quot;&gt;CPU&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;GPU&lt;/code&gt;. Our preprocessing pipeline runs entirely on the &lt;code class=&quot;highlighter-rouge&quot;&gt;GPU&lt;/code&gt; so we’ll be sticking with the &lt;code class=&quot;highlighter-rouge&quot;&gt;GPU&lt;/code&gt; options for this tutorial series.&lt;/p&gt;

&lt;p&gt;Make a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;WorkerFactory.Type&lt;/code&gt; called &lt;code class=&quot;highlighter-rouge&quot;&gt;workerType&lt;/code&gt;. Give it a default value of &lt;code class=&quot;highlighter-rouge&quot;&gt;WorkerFactory.Type.Auto&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\workerType_variable.png&quot; alt=&quot;workerType_variable&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;create-m_runtimemodel-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;m_RuntimeModel&lt;/code&gt; Variable&lt;/h4&gt;

&lt;p&gt;We need to compile the &lt;code class=&quot;highlighter-rouge&quot;&gt;modelAsset&lt;/code&gt; into a run-time model to perform inference. We’ll store the compiled model in a new private &lt;code class=&quot;highlighter-rouge&quot;&gt;Model&lt;/code&gt; variable called &lt;code class=&quot;highlighter-rouge&quot;&gt;m_RuntimeModel&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\m_RuntimeModel_variable.png&quot; alt=&quot;m_RuntimeModel_variable&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;create-engine-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;engine&lt;/code&gt; Variable&lt;/h4&gt;

&lt;p&gt;Next, we’ll create a new private &lt;code class=&quot;highlighter-rouge&quot;&gt;IWorker&lt;/code&gt; variable to store our inference engine. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;engine&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\engine_variable.png&quot; alt=&quot;engine_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;compile-the-model&quot;&gt;Compile the Model&lt;/h3&gt;

&lt;p&gt;We need to get an object oriented representation of the model before we can work with it. We’ll do this in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method and store it in the &lt;code class=&quot;highlighter-rouge&quot;&gt;m_RuntimeModel&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\compile_model.png&quot; alt=&quot;compile_model&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;initialize-inference-engine&quot;&gt;Initialize Inference Engine&lt;/h3&gt;

&lt;p&gt;Now we can create a worker to execute the modified model using the selected backend. We’ll do this using the &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.WorkerFactory.html#Unity_Barracuda_WorkerFactory_CreateWorker_Unity_Barracuda_WorkerFactory_Type_Unity_Barracuda_Model_System_Boolean_&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;WorkerFactory.CreateWorker()&lt;/code&gt;&lt;/a&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\initialize_inference_engine.png&quot; alt=&quot;initialize_inference_engine&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;release-inference-engine-resources&quot;&gt;Release Inference Engine Resources&lt;/h3&gt;

&lt;p&gt;We need to manually release the resources that get allocated for the inference &lt;code class=&quot;highlighter-rouge&quot;&gt;engine&lt;/code&gt;. This should be one of the last actions performed. Therefore, we’ll do it in the &lt;code class=&quot;highlighter-rouge&quot;&gt;OnDisable()&lt;/code&gt; method. This method gets called when the Unity project exits.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\onDisable_method.png&quot; alt=&quot;onDisable_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-totexture2d-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;ToTexture2D()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;We’ll make a new method to copy the data from a &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderTexture&lt;/code&gt; to a new &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt;. We’ll need to call this method before performing both the preprocessing and postprocessing steps. The method will take in the source &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderTexture&lt;/code&gt; and the format for the new &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\toTexture2D_method.png&quot; alt=&quot;toTexture2D_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-processimage-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessImage()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;Next, we’ll make a new method to execute the &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessInput()&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt; functions in our &lt;code class=&quot;highlighter-rouge&quot;&gt;ComputeShader&lt;/code&gt;. This method will take in the image that needs to be processed as well as a function name to indicate which function we want to execute. We’ll need to store the processed images in textures with HDR formats. This will allow us to use color values outside the default range of &lt;code class=&quot;highlighter-rouge&quot;&gt;[0, 1]&lt;/code&gt;. As mentioned previously, the model expects values in the range of &lt;code class=&quot;highlighter-rouge&quot;&gt;[0, 255]&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\processImage_method.png&quot; alt=&quot;processImage_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;process-input-image&quot;&gt;Process Input Image&lt;/h3&gt;

&lt;p&gt;Now we can process the current camera frame. We’ll call the &lt;code class=&quot;highlighter-rouge&quot;&gt;ToTexture2D()&lt;/code&gt; method at the top of the &lt;code class=&quot;highlighter-rouge&quot;&gt;Update&lt;/code&gt; method. The &lt;code class=&quot;highlighter-rouge&quot;&gt;cameraInput&lt;/code&gt; is not an HDR texture so we’ll use an SDR format for the new &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt;. We’ll then call the &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessImage()&lt;/code&gt; method with new &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt; as input.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\process_input_image.png&quot; alt=&quot;process_input_image&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;perform-inference&quot;&gt;Perform Inference&lt;/h3&gt;

&lt;p&gt;Next, we’ll feed the &lt;code class=&quot;highlighter-rouge&quot;&gt;processedImage&lt;/code&gt; to the model and get the output. We first need to convert the &lt;code class=&quot;highlighter-rouge&quot;&gt;processedImage&lt;/code&gt; to a &lt;code class=&quot;highlighter-rouge&quot;&gt;Tensor&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\perform_inference_pt1.png&quot; alt=&quot;perform_inference_pt1&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We’ll then use the &lt;code class=&quot;highlighter-rouge&quot;&gt;engine.Execute()&lt;/code&gt; method to run the model with the current &lt;code class=&quot;highlighter-rouge&quot;&gt;input&lt;/code&gt;. We can store the raw output from the model in a new &lt;code class=&quot;highlighter-rouge&quot;&gt;Tensor&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\perform_inference_pt2.png&quot; alt=&quot;perform_inference_pt2&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;process-the--output&quot;&gt;Process the  Output&lt;/h3&gt;

&lt;p&gt;We need to process the raw output from the model before we can display it to the user. We’ll first copy the model output to a new HDR &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderTexture&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\process_output_pt1.png&quot; alt=&quot;process_output_pt1&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We’ll then copy the data to a &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture2D&lt;/code&gt; and pass it to the &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessImage()&lt;/code&gt; method. This time we’ll be executing the &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt; function on the &lt;code class=&quot;highlighter-rouge&quot;&gt;ComputeShader&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\process_output_pt2.png&quot; alt=&quot;process_output_pt2&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;display-the-processed-output&quot;&gt;Display the Processed Output&lt;/h3&gt;

&lt;p&gt;We can finally display the stylized image by using the &lt;code class=&quot;highlighter-rouge&quot;&gt;Graphics.Blit()&lt;/code&gt; method to copy the final image to &lt;code class=&quot;highlighter-rouge&quot;&gt;processedOutput&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\display_output.png&quot; alt=&quot;display_output&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Next, we’ll need to modify the project scene to use the &lt;code class=&quot;highlighter-rouge&quot;&gt;StyleTransfer&lt;/code&gt; script.&lt;/p&gt;

&lt;h2 id=&quot;open-the-biped-scene&quot;&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;Biped&lt;/code&gt; Scene&lt;/h2&gt;

&lt;p&gt;In the &lt;code class=&quot;highlighter-rouge&quot;&gt;Assets&lt;/code&gt; window, open the &lt;code class=&quot;highlighter-rouge&quot;&gt;Scenes&lt;/code&gt; folder and double-click on the &lt;code class=&quot;highlighter-rouge&quot;&gt;Biped.unity&lt;/code&gt; asset. You don’t need to save the current scene if you get prompted to do so.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\select_biped_scene.png&quot; alt=&quot;select_biped_scene&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;create-style-converter&quot;&gt;Create Style Converter&lt;/h2&gt;

&lt;p&gt;To run the &lt;code class=&quot;highlighter-rouge&quot;&gt;StyleTransfer&lt;/code&gt; script, we need to attach it to a &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; in the scene.&lt;/p&gt;

&lt;h3 id=&quot;create-an-empty-gameobject&quot;&gt;Create an Empty &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;In the Hierarchy tab, right-click an empty space and select &lt;code class=&quot;highlighter-rouge&quot;&gt;Create Empty&lt;/code&gt; from the menu. Name the empty GameObject &lt;code class=&quot;highlighter-rouge&quot;&gt;StyleConverter&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\create_empyt_gameObject.png&quot; alt=&quot;create_empyt_gameObject&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;attach-the-styletransfer-script&quot;&gt;Attach the &lt;code class=&quot;highlighter-rouge&quot;&gt;StyleTransfer&lt;/code&gt; Script&lt;/h3&gt;

&lt;p&gt;With the &lt;code class=&quot;highlighter-rouge&quot;&gt;StyleConverter&lt;/code&gt; object selected, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;StyleTransfer&lt;/code&gt; script into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\attach_styleTransfer_script.png&quot; alt=&quot;attach_styleTransfer_script&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;assign-the-assets&quot;&gt;Assign the Assets&lt;/h3&gt;

&lt;p&gt;We need to assign the render textures, compute shader and one of the ONNX files to their respective parameters in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. I’ll start with the mosaic model. We’ll also set the &lt;code class=&quot;highlighter-rouge&quot;&gt;Worker Type&lt;/code&gt; to &lt;code class=&quot;highlighter-rouge&quot;&gt;Compute Precompiled&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\attach_styleTransfer_script_full.png&quot; alt=&quot;attach_styleTransfer_script_full&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;set-camera-target-texture&quot;&gt;Set Camera Target Texture&lt;/h2&gt;

&lt;p&gt;Select the &lt;code class=&quot;highlighter-rouge&quot;&gt;_Scene&lt;/code&gt; object in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab. In the dropdown, select the &lt;code class=&quot;highlighter-rouge&quot;&gt;Main Camera&lt;/code&gt; object.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\assign_camera_target_texture.png&quot; alt=&quot;assign_camera_target_texture&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;create-a-screen&quot;&gt;Create a Screen&lt;/h2&gt;

&lt;p&gt;Right-click an empty space in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab and select &lt;code class=&quot;highlighter-rouge&quot;&gt;Raw Image&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;UI&lt;/code&gt; submenu. Name it Screen.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\create_raw_image.png&quot; alt=&quot;create_raw_image&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;adjust-the-anchor-presets&quot;&gt;Adjust the Anchor Presets&lt;/h3&gt;

&lt;p&gt;With the &lt;code class=&quot;highlighter-rouge&quot;&gt;Screen&lt;/code&gt; object selected, click on the anchor presets box in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab outlined below.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\stretch_screen_pt1.png&quot; alt=&quot;stretch_screen_pt1&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Select the option in the bottom right corner that’s outlined below.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\stretch_screen_pt2.png&quot; alt=&quot;stretch_screen_pt2&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Next we need to set all the &lt;code class=&quot;highlighter-rouge&quot;&gt;Rect Transform&lt;/code&gt; values to zero. This will cause the &lt;code class=&quot;highlighter-rouge&quot;&gt;Screen&lt;/code&gt; to take up the entire display.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\set_anchors_to_zero.png&quot; alt=&quot;set_anchors_to_zero&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;set-the-screen-texture&quot;&gt;Set the Screen Texture&lt;/h3&gt;

&lt;p&gt;With the Screen object still selected, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessedOutput&lt;/code&gt; asset into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Texture&lt;/code&gt; parameter in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\assign_screen_texture.png&quot; alt=&quot;assign_screen_texture&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;adjust-the-game-tab&quot;&gt;Adjust the Game Tab&lt;/h3&gt;

&lt;p&gt;Our last step is to set up the game tab for our chosen resolution.&lt;/p&gt;

&lt;h4 id=&quot;set-the-aspect-ratio&quot;&gt;Set the Aspect Ratio&lt;/h4&gt;

&lt;p&gt;My chosen resolution of &lt;code class=&quot;highlighter-rouge&quot;&gt;720 x 540&lt;/code&gt; has a &lt;code class=&quot;highlighter-rouge&quot;&gt;4:3&lt;/code&gt; aspect ratio. You can change the aspect ratio in the drop-down menu.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\set_aspect_ratio.png&quot; alt=&quot;set_aspect_ratio&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;disable-warning&quot;&gt;Disable Warning&lt;/h4&gt;

&lt;p&gt;You might see a warning saying that there isn’t a camera rendering. This would be because we set the camera to render to &lt;code class=&quot;highlighter-rouge&quot;&gt;CameraInput&lt;/code&gt;. If you do, right-click the &lt;code class=&quot;highlighter-rouge&quot;&gt;Game&lt;/code&gt; tab and uncheck the &lt;code class=&quot;highlighter-rouge&quot;&gt;Warn if No Cameras Rendering&lt;/code&gt; option.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\basic-in-game-style-transfer-tutorial\disable_no_camera_warning.png&quot; alt=&quot;disable_no_camera_warning&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;test-it-out&quot;&gt;Test it Out&lt;/h2&gt;

&lt;p&gt;We can finally press the play button and see how it looks.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;..\images\basic-in-game-style-transfer-tutorial\basic_in_game_style_transfer_4.gif&quot; alt=&quot;basic_in_game_style_transfer_4&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;

&lt;p&gt;We now have a basic implementation of in-game style transfer. It’s pretty inefficient and probably needs a seizure warning. I started with this model architecture for it’s relative simplicity but it was not designed for real-time video. I was surprised to get playable frame rates even at this low of a resolution.&lt;/p&gt;

&lt;p&gt;Despite it’s shortcomings, this little demo provides a glimpse at what’s possible. It can also serve as a decent testing environment for trying out different styles. It’s worth noting that the models used in this tutorial were trained on datasets of real-world photos and not video games. I might try making an training dataset using screenshots from video games and see what impact that has on the stylized images.&lt;/p&gt;

&lt;p&gt;I already have another style transfer &lt;a href=&quot;https://github.com/OndrejTexler/Few-Shot-Patch-Based-Training&quot;&gt;project&lt;/a&gt; that I want to try to get working in Unity. This project does a great job of generating consistent video output (i.e. no seizure warning). In the mean time, I recommend checking out Unity’s sample &lt;a href=&quot;https://github.com/UnityLabs/barracuda-style-transfer&quot;&gt;project&lt;/a&gt;. They put a lot of work into optimizing it for playable frame rates at more reasonable resolutions.&lt;/p&gt;</content><author><name></name></author><summary type="html">Introduction Select a Unity Project Install Barracuda Package Create Style Transfer Folder Import Models Prepare Render Textures Create a Compute Shader Create StyleTransfer Script Summary</summary></entry><entry><title type="html">How to Flip an Image With a Compute Shader</title><link href="https://christianjmills.com/How-to-Flip-an-Image-With-a-Compute-Shader/" rel="alternate" type="text/html" title="How to Flip an Image With a Compute Shader" /><published>2020-12-03T00:00:00-06:00</published><updated>2020-12-03T00:00:00-06:00</updated><id>https://christianjmills.com/How-to-Flip-an-Image-With-a-Compute-Shader</id><content type="html" xml:base="https://christianjmills.com/How-to-Flip-an-Image-With-a-Compute-Shader/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Dec 3, 2020&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#&quot;&gt;&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;##&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;</content><author><name></name></author><summary type="html">Last Updated: Dec 3, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 8</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-8/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 8" /><published>2020-11-20T00:00:00-06:00</published><updated>2020-11-20T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-8</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-8/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Dec 1, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-7&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-7/&quot;&gt;Part 7&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#add-unityenginevideo-namespace&quot;&gt;Add UnityEngine.Video Namespace&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#add-video-resolution-variables&quot;&gt;Add Video Resolution Variables&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#update-start-method&quot;&gt;Update Start() Method&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#update-processoutput-method&quot;&gt;Update ProcessOutput() Method&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#try-it-out&quot;&gt;Try It Out&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;So far, we’ve only worked with video sources that have &lt;code class=&quot;highlighter-rouge&quot;&gt;16:9&lt;/code&gt; aspect ratios. You might need to work with input that has a taller or wider aspect ratio depending on your application and equipment. In this post, we’ll cover how to adjust to the current source resolution at runtime.&lt;/p&gt;

&lt;h2 id=&quot;add-unityenginevideo-namespace&quot;&gt;Add &lt;code class=&quot;highlighter-rouge&quot;&gt;UnityEngine.Video&lt;/code&gt; Namespace&lt;/h2&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script and add a new &lt;a href=&quot;https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/using-directive&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;using directive&lt;/code&gt;&lt;/a&gt; at the top. We need the &lt;code class=&quot;highlighter-rouge&quot;&gt;UnityEngine.Video&lt;/code&gt; namespace to work with the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; object.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\import_unityengine_video.png&quot; alt=&quot;import_unityengine_video&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;add-webcam-variables&quot;&gt;Add Webcam Variables&lt;/h2&gt;

&lt;p&gt;Unity defaults to a resolution of &lt;code class=&quot;highlighter-rouge&quot;&gt;640 x 480&lt;/code&gt; for webcams. There is no built-in method that returns the frame rate. However, the default does not appear to be over 30fps.  We can request a resolution and frame rate when initializing the &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt;. Unity should accept the requested settings as long as the camera supports them. Unity defaults to its closest preset if the requested settings are not supported.&lt;/p&gt;

&lt;h3 id=&quot;create-webcamheight-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamHeight&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Add a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;int&lt;/code&gt; variable so we can adjust the camera height from the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamHeight&lt;/code&gt;. My webcam supports 720p at 60fps, so I’ve set the default value to &lt;code class=&quot;highlighter-rouge&quot;&gt;720&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\webcamHeight_variable.png&quot; alt=&quot;webcamHeight_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-webcamwidth-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamWidth&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Next, create a variable for the camera’s width and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamWidth&lt;/code&gt;. I’ve set the default value to &lt;code class=&quot;highlighter-rouge&quot;&gt;1280&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\webcamWidth_variable.png&quot; alt=&quot;webcamWidth_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-webcamfps-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamFPS&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We’ll also add a variable to set the frame rate for the camera and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamFPS&lt;/code&gt;. Set the default value to &lt;code class=&quot;highlighter-rouge&quot;&gt;60&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\webcam_fps_variable.png&quot; alt=&quot;webcam_fps_variable&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;add-video-resolution-variables&quot;&gt;Add Video Resolution Variables&lt;/h2&gt;

&lt;p&gt;Next, we need to create a couple of private &lt;code class=&quot;highlighter-rouge&quot;&gt;int&lt;/code&gt; variables to store the dimensions of the video source. Name the variables &lt;code class=&quot;highlighter-rouge&quot;&gt;videoHeight&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;videoWidth&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\video_resolution_variables.png&quot; alt=&quot;video_resolution_variables&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;update-start-method&quot;&gt;Update &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; Method&lt;/h2&gt;

&lt;p&gt;We’ll make some modifications to the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method to prepare for input with different aspect ratios.&lt;/p&gt;

&lt;h3 id=&quot;get-reference-to-video-player&quot;&gt;Get Reference to &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;Create a new &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; variable to access the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; object. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;videoPlayer&lt;/code&gt; and place it at the top of the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method. We’ll also move the &lt;code class=&quot;highlighter-rouge&quot;&gt;Transform videoScreen&lt;/code&gt; variable outside of the &lt;code class=&quot;highlighter-rouge&quot;&gt;if (useWebcam)&lt;/code&gt; statement.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\find_video_player.png&quot; alt=&quot;find_video_player&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;get-webcam-resolution&quot;&gt;Get Webcam Resolution&lt;/h3&gt;

&lt;p&gt;We can’t access the webcam resolution until after the webcam has started. Therefore, we’ll update the values for &lt;code class=&quot;highlighter-rouge&quot;&gt;videoHeight&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;videoWidth&lt;/code&gt; after &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture.Play()&lt;/code&gt; has been called.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\get_webcam_resolution.png&quot; alt=&quot;get_webcam_resolution&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;get-video-clip-dimensions&quot;&gt;Get Video Clip Dimensions&lt;/h3&gt;

&lt;p&gt;We need to get the video resolution from the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; object when we’re not using a webcam. We can get the height and width by accessing the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoPlayer&lt;/code&gt; component.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\get_video_clip_dimensions.png&quot; alt=&quot;get_video_clip_dimensions&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;replace-videotexture&quot;&gt;Replace &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;The dimensions for &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; can’t be updated once it’s been created. We’ll need to replace it with a new &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderTexture&lt;/code&gt; that uses the new &lt;code class=&quot;highlighter-rouge&quot;&gt;videoHeight&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;videoWidth&lt;/code&gt; values. We also need to set the new &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; as the &lt;code class=&quot;highlighter-rouge&quot;&gt;targetTexture&lt;/code&gt; for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; object.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\replace_videoTexture.png&quot; alt=&quot;replace_videoTexture&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;update-videoscreen&quot;&gt;Update &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;Next, we need to apply the new &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; to the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; object. We’ll also resize and reposition the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; to account for any changes in the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; dimensions.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\update_videoScreen_2.png&quot; alt=&quot;update_videoScreen&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;adjust-main-camera&quot;&gt;Adjust &lt;code class=&quot;highlighter-rouge&quot;&gt;Main Camera&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;We’ll also resize and reposition the &lt;code class=&quot;highlighter-rouge&quot;&gt;Main Camera&lt;/code&gt; object to account for changes to the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\adjust_main_camera.png&quot; alt=&quot;adjust_main_camera&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;update-processoutput-method&quot;&gt;Update &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt; Method&lt;/h2&gt;

&lt;p&gt;Finally, we need to modify how we calculate the key point locations.&lt;/p&gt;

&lt;h3 id=&quot;update-scaling-calculations&quot;&gt;Update Scaling Calculations&lt;/h3&gt;

&lt;p&gt;We’ll account for different aspect ratios by first determining whether the height or width of &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; is larger. We then use that information to calculate the &lt;code class=&quot;highlighter-rouge&quot;&gt;scale&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;unsqueezeScale&lt;/code&gt; values.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\update_scaling_calculations.png&quot; alt=&quot;update_scaling_calculations&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;update-key-point-calculations&quot;&gt;Update Key Point Calculations&lt;/h3&gt;

&lt;p&gt;We should only scale the &lt;code class=&quot;highlighter-rouge&quot;&gt;xPos&lt;/code&gt; value by the &lt;code class=&quot;highlighter-rouge&quot;&gt;unsqueezeScale&lt;/code&gt; if the width larger than the height. Remove that part from the initial &lt;code class=&quot;highlighter-rouge&quot;&gt;xPos&lt;/code&gt; calculation. We also need to move the &lt;code class=&quot;highlighter-rouge&quot;&gt;if (useWebcam)&lt;/code&gt; statement.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\update_key_point_calculations_part1v2.png&quot; alt=&quot;update_key_point_calculations_part1&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We’ll scale either &lt;code class=&quot;highlighter-rouge&quot;&gt;xPos&lt;/code&gt; or &lt;code class=&quot;highlighter-rouge&quot;&gt;yPos&lt;/code&gt; by &lt;code class=&quot;highlighter-rouge&quot;&gt;unsqueezeScale&lt;/code&gt; depending on whether the width or height value for &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; is larger. Place the &lt;code class=&quot;highlighter-rouge&quot;&gt;if (useWebcam)&lt;/code&gt; statement after this calculation.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\update_key_point_calculations_part2.png&quot; alt=&quot;update_key_point_calculations_part2&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;try-it-out&quot;&gt;Try It Out&lt;/h2&gt;

&lt;p&gt;Now we can test the modifications on some vertical videos from &lt;a href=&quot;https://www.pexels.com/&quot;&gt;Pexels&lt;/a&gt;.&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;a href=&quot;https://www.pexels.com/video/woman-doing-a-jump-rope-exercise-2785536/&quot;&gt;Woman Doing a Jump Rope Exercise&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://www.pexels.com/video/man-dancing-hip-hop-2795742/&quot;&gt;Man Dancing Hip-Hop&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-8\jump_rope_pose_estimation_4.gif&quot; alt=&quot;jump_rope_pose_estimation_4&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;

&lt;p&gt;Now we can use video sources with different aspect ratios without needing to manually change any parameters.&lt;/p&gt;</content><author><name></name></author><summary type="html">Last Updated: Dec 1, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 7</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-7/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 7" /><published>2020-11-15T00:00:00-06:00</published><updated>2020-11-15T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-7</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-7/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Dec 1, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-6&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-6/&quot;&gt;Part 6&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#modify-posenet-script&quot;&gt;Modify PoseNet Script&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#set-inspector-variable&quot;&gt;Set Inspector Variable&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;It’s time to see how our model performs with a webcam. Prerecorded videos are great for testing, but most real-world applications will likely use a live video feed. We’ll set up our video feed for a front-facing camera that mirrors the user.&lt;/p&gt;

&lt;h2 id=&quot;modify-posenet-script&quot;&gt;Modify &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; Script&lt;/h2&gt;

&lt;p&gt;We can add the option to use a webcam feed by making some modifications to the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script.&lt;/p&gt;

&lt;h3 id=&quot;create-usewebcam-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;useWebcam&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script and create a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;bool&lt;/code&gt; variable. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;useWebcam&lt;/code&gt; and set the default value to &lt;code class=&quot;highlighter-rouge&quot;&gt;false&lt;/code&gt;. This will create a checkbox in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab that we can use to enable and disable the webcam.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-7\useWebcam_variable.png&quot; alt=&quot;useWebcam_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-webcamtexture-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We’ll use a &lt;a href=&quot;https://docs.unity3d.com/ScriptReference/WebCamTexture.html&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;WebCamTexture&lt;/code&gt;&lt;/a&gt; variable to store the live video input from our webcam. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-7\webcamTexture_variable.png&quot; alt=&quot;webcamTexture_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;set-up-webcam-feed&quot;&gt;Set Up Webcam Feed&lt;/h3&gt;

&lt;p&gt;We’ll prepare the webcam feed at the top of the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method. You can find the completed code below.&lt;/p&gt;

&lt;h4 id=&quot;initialize-the-webcamtexture&quot;&gt;Initialize the &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt;&lt;/h4&gt;

&lt;p&gt;First, initialize the &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt;. We’ll use the first video input device Unity finds. If you have more than one webcam attached, you’ll need to &lt;a href=&quot;https://docs.unity3d.com/ScriptReference/WebCamTexture-ctor.html&quot;&gt;specify&lt;/a&gt; the device name.&lt;/p&gt;

&lt;h4 id=&quot;flip-the-videoscreen&quot;&gt;Flip the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt;&lt;/h4&gt;

&lt;p&gt;Next, we need to adjust the rotation and scale of the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; object. The webcam feed doesn’t mirror the user by default. For example, the user’s right arm appears on the left side of the screen. This can be disorienting when looking at the generated pose skeleton. We’ll flip the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; to compensate.&lt;/p&gt;

&lt;h4 id=&quot;start-the-camera&quot;&gt;Start the Camera&lt;/h4&gt;

&lt;p&gt;We’ll use the &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture.Play()&lt;/code&gt; method to start the camera.&lt;/p&gt;

&lt;h4 id=&quot;deactivate-the-video-player&quot;&gt;Deactivate the Video Player&lt;/h4&gt;

&lt;p&gt;Finally, we’ll deactivate the &lt;code class=&quot;highlighter-rouge&quot;&gt;Video Player&lt;/code&gt; as it’s not being used.&lt;/p&gt;

&lt;h4 id=&quot;completed-code&quot;&gt;Completed Code&lt;/h4&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-7\initialize_webcam_start_method.png&quot; alt=&quot;initialize_webcam_start_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;get-webcamtexture-data&quot;&gt;Get &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt; Data&lt;/h3&gt;

&lt;p&gt;We’ll use the &lt;code class=&quot;highlighter-rouge&quot;&gt;Graphics.Blit()&lt;/code&gt; method to update the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; with the data from &lt;code class=&quot;highlighter-rouge&quot;&gt;webcamTexture&lt;/code&gt;. Add the following code at the top of the &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-7\useWebcam_update_method.png&quot; alt=&quot;useWebcam_update_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;flip-key-point-locations&quot;&gt;Flip Key Point Locations&lt;/h3&gt;

&lt;p&gt;Flipping the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; does not flip the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; itself. Therefore, the output of the model will not be flipped either. We can fix this by mirroring the &lt;code class=&quot;highlighter-rouge&quot;&gt;xPos&lt;/code&gt; values for the calculated key point locations.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-7\useWebcam_processOutput_method.png&quot; alt=&quot;useWebcam_processOutput_method&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;set-inspector-variable&quot;&gt;Set Inspector Variable&lt;/h2&gt;

&lt;p&gt;Now we can enable and disable the webcam from the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-7\enable_useWebcam_inspector.PNG&quot; alt=&quot;enable_useWebcam_inspector&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Note:&lt;/code&gt; Don’t toggle the &lt;code class=&quot;highlighter-rouge&quot;&gt;useWebcam&lt;/code&gt; parameter during runtime with the code as it is.&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;

&lt;p&gt;We can now perform pose estimation using either prerecorded or live video feeds. We’ll further increase our flexibility for input sources in the next post by adding the ability to handle input with different aspect ratios.&lt;/p&gt;

&lt;h3 id=&quot;next-part-8&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-8/&quot;&gt;Part 8&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Dec 1, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 6</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-6/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 6" /><published>2020-11-14T00:00:00-06:00</published><updated>2020-11-14T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-6</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-6/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 30, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-5&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-5/&quot;&gt;Part 5&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-drawskeleton-script&quot;&gt;Create DrawSkeleton Script&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-skeletonrenderer&quot;&gt;Create SkeletonRenderer&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#try-it-out&quot;&gt;Try It Out&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;In this post, we’ll add some connecting lines between the key points to create a simple skeleton. This will improve visibility over the key point objects alone.&lt;/p&gt;

&lt;h2 id=&quot;create-drawskeleton-script&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;DrawSkeleton&lt;/code&gt; Script&lt;/h2&gt;

&lt;p&gt;We’ll complete our pose skeleton by drawing lines connecting the appropriate key points. Create a new &lt;code class=&quot;highlighter-rouge&quot;&gt;C#&lt;/code&gt; script and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;DrawSkeleton&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\create_drawSkeleton_script.PNG&quot; alt=&quot;create_drawSkeleton_script&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-keypoints-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;keypoints&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We need to access the key point objects so make another public &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; array just like in the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\keypoints_variable_drawSkeleton.png&quot; alt=&quot;keypoints_variable_drawSkeleton&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-lines-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;lines&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Next, create a private &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; array to hold the lines themselves. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;lines&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\lines_variable.png&quot; alt=&quot;lines_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-linerenderers-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;lineRenderers&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We’ll use &lt;a href=&quot;https://docs.unity3d.com/Manual/class-LineRenderer.html&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;LineRenderer&lt;/code&gt;&lt;/a&gt; components to draw the skeleton.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\lineRenderers_variable.png&quot; alt=&quot;lineRenderers_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-jointpairs-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;jointPairs&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;The next variable will contain pairs of key point indices. The corresponding key points indicate the start and end points for the skeleton lines.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\jointPairs_variable.png&quot; alt=&quot;jointPairs_variable&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;joint-pairs&quot;&gt;Joint Pairs&lt;/h4&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;#&lt;/th&gt;
      &lt;th&gt;Starting Key Point&lt;/th&gt;
      &lt;th&gt;Ending Key Point&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;1&lt;/td&gt;
      &lt;td&gt;Nose&lt;/td&gt;
      &lt;td&gt;Left Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;2&lt;/td&gt;
      &lt;td&gt;Nose&lt;/td&gt;
      &lt;td&gt;Right Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;3&lt;/td&gt;
      &lt;td&gt;Left Eye&lt;/td&gt;
      &lt;td&gt;Left Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;4&lt;/td&gt;
      &lt;td&gt;Right Eye&lt;/td&gt;
      &lt;td&gt;Right Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;5&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;6&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;7&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;8&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;9&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;10&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;11&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
      &lt;td&gt;Left Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;12&lt;/td&gt;
      &lt;td&gt;Left Elbow&lt;/td&gt;
      &lt;td&gt;Left Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;13&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
      &lt;td&gt;Right Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;14&lt;/td&gt;
      &lt;td&gt;Right Elbow&lt;/td&gt;
      &lt;td&gt;Right Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;15&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
      &lt;td&gt;Left Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;16&lt;/td&gt;
      &lt;td&gt;Left Knee&lt;/td&gt;
      &lt;td&gt;Left Ankle&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;17&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
      &lt;td&gt;Right Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;18&lt;/td&gt;
      &lt;td&gt;Right Knee&lt;/td&gt;
      &lt;td&gt;Right Ankle&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;h3 id=&quot;create-linewidth-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;lineWidth&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;The last variable we’ll make defines the line width.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\lineWidth_variable.png&quot; alt=&quot;lineWidth_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;initialize-variables&quot;&gt;Initialize Variables&lt;/h3&gt;

&lt;p&gt;We need to initialize the &lt;code class=&quot;highlighter-rouge&quot;&gt;lines&lt;/code&gt;, &lt;code class=&quot;highlighter-rouge&quot;&gt;lineRenderers&lt;/code&gt;, and &lt;code class=&quot;highlighter-rouge&quot;&gt;jointPairs&lt;/code&gt; variables in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\initialize_drawSkeleton_variables.png&quot; alt=&quot;initialize_drawSkeleton_variables&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-initializeline-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;InitializeLine()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;We’ll create a new method to set up each of the lines in the pose skeleton. The method will create an empty &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; for a line and add a &lt;code class=&quot;highlighter-rouge&quot;&gt;LineRenderer&lt;/code&gt; component to it. We won’t set the start and end positions as none of the key points will have updated yet.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\initializeLine_method.png&quot; alt=&quot;initializeLine_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-initializeskeleton-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;InitializeSkeleton()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;Next, we need to call &lt;code class=&quot;highlighter-rouge&quot;&gt;InitializeLine()&lt;/code&gt; in a new method for each line in the pose skeleton. We’ll give each region of the skeleton a different color.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\initializeSkeleton_method.png&quot; alt=&quot;initializeSkeleton_method&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;call-the-method&quot;&gt;Call the method&lt;/h4&gt;

&lt;p&gt;We’ll initialize the pose skeleton lines in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\call_initializeSkeleton_method.png&quot; alt=&quot;call_initializeSkeleton_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-renderskeleton-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;RenderSkeleton()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;The last method we need to define will handle updating the position of the each of the lines in the pose skeleton. The method will iterate through each of the joint pairs and update the start and end positions for the associated &lt;code class=&quot;highlighter-rouge&quot;&gt;LineRenderer&lt;/code&gt;. We’ll only display a given line if both of the key point objects are currently active.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\renderSkeleton_method.png&quot; alt=&quot;renderSkeleton_method&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;call-the-method-1&quot;&gt;Call the method&lt;/h4&gt;

&lt;p&gt;We’ll render the skeleton lines in the &lt;code class=&quot;highlighter-rouge&quot;&gt;LateUpdate()&lt;/code&gt; method instead of &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt;. This will ensure the PoseNet model has run for the latest frame before updating the pose skeleton.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\call_renderSkeleton_method_2.png&quot; alt=&quot;call_renderSkeleton_method&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;create-skeletonrenderer&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;SkeletonRenderer&lt;/code&gt;&lt;/h2&gt;

&lt;p&gt;We’ll attach the &lt;code class=&quot;highlighter-rouge&quot;&gt;DrawSkeleton&lt;/code&gt; script to a new &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt;. Create an empty &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;SkeletonRenderer&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\create_skeletonRenderer_object.PNG&quot; alt=&quot;create_skeletonRenderer_object&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;attach-the-drawskeleton-script&quot;&gt;Attach the &lt;code class=&quot;highlighter-rouge&quot;&gt;DrawSkeleton&lt;/code&gt; Script&lt;/h3&gt;

&lt;p&gt;With &lt;code class=&quot;highlighter-rouge&quot;&gt;SkeletonRenderer&lt;/code&gt; selected in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt;, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;DrawSkeleton&lt;/code&gt; script into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\skeletonRenderer_inspector_empty.PNG&quot; alt=&quot;skeletonRenderer_inspector_empty&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;assign-key-points&quot;&gt;Assign Key Points&lt;/h3&gt;

&lt;p&gt;Drag and drop the key point objects onto the &lt;code class=&quot;highlighter-rouge&quot;&gt;Keypoints&lt;/code&gt; parameter just like with the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\skeletonRenderer_inspector_full.PNG&quot; alt=&quot;skeletonRenderer_inspector_full&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;try-it-out&quot;&gt;Try It Out&lt;/h2&gt;

&lt;p&gt;If you press the play button, you should see something like this.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th&gt;Variable&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;Value&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;imageHeight&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;720&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;imageWidth&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;720&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;minConfidence&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;85&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-6\pose_skeleton_480p_90c.gif&quot; alt=&quot;pose_skeleton_480p_90c&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;

&lt;p&gt;We now have a complete pose skeleton that we can use for debugging or demos. In the next post, we’ll add the option to use a live webcam feed as input instead of a prerecorded video.&lt;/p&gt;

&lt;h3 id=&quot;next-part--7&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-7/&quot;&gt;Part  7&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 30, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 5</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-5/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 5" /><published>2020-11-13T00:00:00-06:00</published><updated>2020-11-13T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-5</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-5/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 30, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-4&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-4/&quot;&gt;Part 4&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-key-points&quot;&gt;Create Key Points&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#map-key-point-locations&quot;&gt;Map Key Point Locations&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;In this post, we’ll make use of the &lt;code class=&quot;highlighter-rouge&quot;&gt;VideoScreen&lt;/code&gt; we made in &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-1/&quot;&gt;Part 1&lt;/a&gt; by checking if the estimated key point locations align with the actual locations in the video. We’ll know everything is working as intended if the objects get placed in front of the target key points. If not, we’ll at least have a visual debugging tool to work out where we things went wrong.&lt;/p&gt;

&lt;h2 id=&quot;create-key-points&quot;&gt;Create Key Points&lt;/h2&gt;

&lt;p&gt;We need to create a separate &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; for each of the &lt;code class=&quot;highlighter-rouge&quot;&gt;17&lt;/code&gt; key points.&lt;/p&gt;

&lt;h3 id=&quot;create-container&quot;&gt;Create Container&lt;/h3&gt;

&lt;p&gt;In the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab, create an empty &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;Key Points&lt;/code&gt;. We’ll store the key point objects in here to keep things organized.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Optional:&lt;/strong&gt; With &lt;code class=&quot;highlighter-rouge&quot;&gt;Key Points&lt;/code&gt; selected, right-click the &lt;code class=&quot;highlighter-rouge&quot;&gt;Transform&lt;/code&gt; component in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. Click &lt;code class=&quot;highlighter-rouge&quot;&gt;Reset&lt;/code&gt; in the pop-up menu. This will reset the object’s position to the origin.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\reset_transform.PNG&quot; alt=&quot;reset_transform&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-gameobjects&quot;&gt;Create GameObjects&lt;/h3&gt;

&lt;p&gt;Right-click the &lt;code class=&quot;highlighter-rouge&quot;&gt;Key Points&lt;/code&gt; object and select &lt;code class=&quot;highlighter-rouge&quot;&gt;Sphere&lt;/code&gt; under &lt;code class=&quot;highlighter-rouge&quot;&gt;3D Object&lt;/code&gt;. This will create a nested &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; inside &lt;code class=&quot;highlighter-rouge&quot;&gt;Key Points&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\create_keypoint_gameobject.PNG&quot; alt=&quot;create_keypoint_gameobject&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Select the new &lt;code class=&quot;highlighter-rouge&quot;&gt;Sphere&lt;/code&gt; object and press Ctrl-d to duplicate it. We’ll need &lt;code class=&quot;highlighter-rouge&quot;&gt;17&lt;/code&gt; spheres total.&lt;/p&gt;

&lt;p&gt;Rename the &lt;code class=&quot;highlighter-rouge&quot;&gt;Sphere&lt;/code&gt; objects according to the table below.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th&gt;Index&lt;/th&gt;
      &lt;th&gt;Name&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;0&lt;/td&gt;
      &lt;td&gt;Nose&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;1&lt;/td&gt;
      &lt;td&gt;Left Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;2&lt;/td&gt;
      &lt;td&gt;Right Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;3&lt;/td&gt;
      &lt;td&gt;Left Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;4&lt;/td&gt;
      &lt;td&gt;Right Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;5&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;6&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;7&lt;/td&gt;
      &lt;td&gt;Left Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;8&lt;/td&gt;
      &lt;td&gt;Right Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;9&lt;/td&gt;
      &lt;td&gt;Left Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;10&lt;/td&gt;
      &lt;td&gt;Right Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;11&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;12&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;13&lt;/td&gt;
      &lt;td&gt;Left Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;14&lt;/td&gt;
      &lt;td&gt;Right Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;15&lt;/td&gt;
      &lt;td&gt;Left Ankle&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;16&lt;/td&gt;
      &lt;td&gt;Right Ankle&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;h4 id=&quot;result&quot;&gt;Result&lt;/h4&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\keypoint_gameobjects.PNG&quot; alt=&quot;keypoint_gameobjects&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;resize-gameobjects&quot;&gt;Resize GameObjects&lt;/h3&gt;

&lt;p&gt;Next, we’ll make the key point objects larger so that they’re easier to see. Select the &lt;code class=&quot;highlighter-rouge&quot;&gt;Nose&lt;/code&gt; object in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierachy&lt;/code&gt;. Then,   hold Shift and click &lt;code class=&quot;highlighter-rouge&quot;&gt;RightAnkle&lt;/code&gt; to select all 17 objects at once.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\select_all_keypoint_objects.PNG&quot; alt=&quot;select_all_keypoint_objects&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We need to increase the &lt;code class=&quot;highlighter-rouge&quot;&gt;X&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;Y&lt;/code&gt; values for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Scale&lt;/code&gt; parameter in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. Increasing them to 10 should be enough.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\increase_key_point_object_size.PNG&quot; alt=&quot;increase_key_point_object_size&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;change-gameobject-material&quot;&gt;Change GameObject Material&lt;/h3&gt;

&lt;p&gt;The default color for a &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; doesn’t stand out much against the background. We’ll make the key point objects yellow since it’s apparently really easy for humans to spot.&lt;/p&gt;

&lt;h4 id=&quot;create-yellow-material&quot;&gt;Create Yellow Material&lt;/h4&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;Materials&lt;/code&gt; folder in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Assets&lt;/code&gt; window. Right-click an empty space and select &lt;code class=&quot;highlighter-rouge&quot;&gt;Material&lt;/code&gt; in the the &lt;code class=&quot;highlighter-rouge&quot;&gt;Create&lt;/code&gt; sub-menu. Name the new material &lt;code class=&quot;highlighter-rouge&quot;&gt;Yellow&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\create_material.PNG&quot; alt=&quot;create_material&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;change-material-color&quot;&gt;Change Material Color&lt;/h4&gt;

&lt;p&gt;With the &lt;code class=&quot;highlighter-rouge&quot;&gt;Yellow&lt;/code&gt; material selected, click the small white box in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. A &lt;code class=&quot;highlighter-rouge&quot;&gt;Color&lt;/code&gt; window should pop up.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\select_material_color_3.png&quot; alt=&quot;select_material_color&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Set the value for &lt;code class=&quot;highlighter-rouge&quot;&gt;B&lt;/code&gt; to &lt;code class=&quot;highlighter-rouge&quot;&gt;0&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Color&lt;/code&gt; window. This will change the color to pure yellow.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\change_material_color_to_yellow.PNG&quot; alt=&quot;change_material_color_to_yellow&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;make-material-unlit&quot;&gt;Make Material Unlit&lt;/h4&gt;

&lt;p&gt;We’ll change the &lt;code class=&quot;highlighter-rouge&quot;&gt;Shader&lt;/code&gt; for the material to &lt;code class=&quot;highlighter-rouge&quot;&gt;Unlit/Color&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\change_material_shader_to_unlit_color.PNG&quot; alt=&quot;change_material_shader_to_unlit_color&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;assign-yellow-material&quot;&gt;Assign Yellow Material&lt;/h4&gt;

&lt;p&gt;Select all the key point objects in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab. Then, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;Yellow&lt;/code&gt; material into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\assign_yellow_material.PNG&quot; alt=&quot;assign_yellow_material&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;map-key-point-locations&quot;&gt;Map Key Point Locations&lt;/h2&gt;

&lt;p&gt;Now we can update the positions of the key point objects using the location data obtained from the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; model. Ordinarily, we would implement this in a separate &lt;code class=&quot;highlighter-rouge&quot;&gt;C#&lt;/code&gt; script. This script would access the &lt;code class=&quot;highlighter-rouge&quot;&gt;keypointLocations[][]&lt;/code&gt; array in the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script. However, we’ll do it in the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script to keep things simple.&lt;/p&gt;

&lt;h3 id=&quot;create-keypoints-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;keypoints&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script and add a public &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt; array. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;keypoints&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\keypoints_variable.png&quot; alt=&quot;keypoints_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;assign-the-key-point-objects&quot;&gt;Assign the Key Point Objects&lt;/h3&gt;

&lt;p&gt;Select the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseEstimator&lt;/code&gt; object in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt; tab. Then, click the small lock icon above the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. This will lock the current selected object in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\lock_inspector_2.png&quot; alt=&quot;lock_inspector_2&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Make sure the &lt;code class=&quot;highlighter-rouge&quot;&gt;Size&lt;/code&gt; value for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Keypoints&lt;/code&gt; variable is set to &lt;code class=&quot;highlighter-rouge&quot;&gt;0&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\initialize_keypoints_parameter.png&quot; alt=&quot;initialize_keypoints_parameter&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Select all the key point objects in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Hierarchy&lt;/code&gt;. Then, drag and drop them onto the &lt;code class=&quot;highlighter-rouge&quot;&gt;Keypoints&lt;/code&gt; parameter in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\assign_keypoint_objects.PNG&quot; alt=&quot;assign_keypoint_objects&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Go ahead and unlock the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab by clicking the lock icon again.&lt;/p&gt;

&lt;h3 id=&quot;create-minconfidence-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;minConfidence&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Next, we’ll add a public &lt;code class=&quot;highlighter-rouge&quot;&gt;int&lt;/code&gt; variable. This variable will define the confidence threshold for deciding whether or not to display a given key point object. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;minConfidence&lt;/code&gt; and set the default value to 70. You can add a &lt;code class=&quot;highlighter-rouge&quot;&gt;Range&lt;/code&gt; attribute to create a slider in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. Set the range to &lt;code class=&quot;highlighter-rouge&quot;&gt;[0, 100]&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\minConfidence_variable_2.png&quot; alt=&quot;minConfidence_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-updatekeypointpositions-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;UpdateKeyPointPositions()&lt;/code&gt; Method&lt;/h3&gt;

&lt;p&gt;We need to define a new method to update the key point positions. Name the method &lt;code class=&quot;highlighter-rouge&quot;&gt;UpdateKeyPointPositions()&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\updateKeyPointPositions_method_2.png&quot; alt=&quot;updateKeyPointPositions_method&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;call-the-method&quot;&gt;Call the Method&lt;/h3&gt;

&lt;p&gt;We’ll call the method in &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt; just after &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-5\call_updateKeyPointPositions_method_2.png&quot; alt=&quot;call_updateKeyPointPositions_method&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;

&lt;p&gt;We now have a useful debugging tool to gauge our model’s performance. In the next post, we’ll create a complete pose skeleton by drawing lines connecting the key point objects we made in this post.&lt;/p&gt;

&lt;h3 id=&quot;next-part-6&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-6/&quot;&gt;Part 6&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 30, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 4</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-4/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 4" /><published>2020-11-12T00:00:00-06:00</published><updated>2020-11-12T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-4</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-4/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 30, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-3&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-3/&quot;&gt;Part 3&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#create-processoutput-method&quot;&gt;Create ProcessOutput() Method&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#calculate-scaling-values&quot;&gt;Calculate Scaling Values&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#locate-key-point-indices&quot;&gt;Locate Key Point Indices&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#calculate-key-point-positions&quot;&gt;Calculate Key Point Positions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;The post processing phase consists of a few main steps. We need to first determine the region of the image that the model estimates is most likely to contain a given key point. We’ll then refine this estimate using the output from the &lt;code class=&quot;highlighter-rouge&quot;&gt;offsetsLayer&lt;/code&gt;. Lastly, we’ll account for any changes in aspect ratio and scale the key point locations up to the source resolution.&lt;/p&gt;

&lt;p&gt;So far, major operations have been performed on the GPU. We’ll be performing the post processing steps on the CPU. &lt;code class=&quot;highlighter-rouge&quot;&gt;Tensor&lt;/code&gt; elements need to be accessed on the main thread. Just reading the values from the model’s output layers forces the rest of the program to wait until the operation completes. Even if we perform the post processing on the GPU, we would still need to access the result on the CPU. I’m working on a way to avoid reading the values on the CPU. Unfortunately, it’s still too messy to include in this tutorial.&lt;/p&gt;

&lt;h2 id=&quot;create-processoutput-method&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt; Method&lt;/h2&gt;

&lt;p&gt;The post processing steps will be handled in a new method called &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt;. The method will take in the output &lt;code class=&quot;highlighter-rouge&quot;&gt;Tensors&lt;/code&gt; from the &lt;code class=&quot;highlighter-rouge&quot;&gt;predictionLayer&lt;/code&gt; and the &lt;code class=&quot;highlighter-rouge&quot;&gt;offsetsLayer&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\processoutput_method_empty.png&quot; alt=&quot;processoutput_method_empty&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Before filling out the function, we need to create a new constant and a new variable.&lt;/p&gt;

&lt;h3 id=&quot;create-numkeypoints-constant&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;numKeypoints&lt;/code&gt; Constant&lt;/h3&gt;

&lt;p&gt;The PoseNet model estimates the 2D locations of &lt;code class=&quot;highlighter-rouge&quot;&gt;17&lt;/code&gt; key points on a human body.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th&gt;Index&lt;/th&gt;
      &lt;th&gt;Name&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;0&lt;/td&gt;
      &lt;td&gt;Nose&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;1&lt;/td&gt;
      &lt;td&gt;Left Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;2&lt;/td&gt;
      &lt;td&gt;Right Eye&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;3&lt;/td&gt;
      &lt;td&gt;Left Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;4&lt;/td&gt;
      &lt;td&gt;Right Ear&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;5&lt;/td&gt;
      &lt;td&gt;Left Shoulder&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;6&lt;/td&gt;
      &lt;td&gt;Right Shoulder&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;7&lt;/td&gt;
      &lt;td&gt;Left Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;8&lt;/td&gt;
      &lt;td&gt;Right Elbow&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;9&lt;/td&gt;
      &lt;td&gt;Left Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;10&lt;/td&gt;
      &lt;td&gt;Right Wrist&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;11&lt;/td&gt;
      &lt;td&gt;Left Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;12&lt;/td&gt;
      &lt;td&gt;Right Hip&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;13&lt;/td&gt;
      &lt;td&gt;Left Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;14&lt;/td&gt;
      &lt;td&gt;Right Knee&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;15&lt;/td&gt;
      &lt;td&gt;Left Ankle&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;16&lt;/td&gt;
      &lt;td&gt;Right Ankle&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Since the number of key points never changes, we’ll store it in an &lt;code class=&quot;highlighter-rouge&quot;&gt;int&lt;/code&gt; constant. Name the constant &lt;code class=&quot;highlighter-rouge&quot;&gt;numKeypoints&lt;/code&gt; and set the value to &lt;code class=&quot;highlighter-rouge&quot;&gt;17&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\numKeypoints_constant.png&quot; alt=&quot;numKeypoints_constant&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-keypointlocations-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;keypointLocations&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;The processed output from the model will be stored in a new variable called &lt;code class=&quot;highlighter-rouge&quot;&gt;keypointLocations&lt;/code&gt;. This variable will contain the &lt;code class=&quot;highlighter-rouge&quot;&gt;(x,y)&lt;/code&gt; coordinates for each key point. For this tutorial, the coordinates will be scaled to the original resolution of &lt;code class=&quot;highlighter-rouge&quot;&gt;1920x1080&lt;/code&gt; for &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;This variable will also store the confidence values associated with the coordinates. The model predicts key point locations even when there isn’t a human in the input image. In such situations, the confidence values will likely be quite low. We can decide how to handle the latest coordinates based on a confidence threshold that we pick.&lt;/p&gt;

&lt;p&gt;There are many ways we can store this information. For simplicity, we’ll stick with an array of arrays. The array will have &lt;code class=&quot;highlighter-rouge&quot;&gt;17&lt;/code&gt; elements. Each element will contain the location information for the key point that matches their index.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\keypointLocations_variable.png&quot; alt=&quot;numKeyPoints_and_keypointLocations&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;retrieve-output-tenors&quot;&gt;Retrieve Output Tenors&lt;/h3&gt;

&lt;p&gt;Call &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt; after &lt;code class=&quot;highlighter-rouge&quot;&gt;engine.Execute(input)&lt;/code&gt; in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt;  method. We’ll use the &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.IWorker.html#Unity_Barracuda_IWorker_PeekOutput_System_String_&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;engine.PeekOutput()&lt;/code&gt;&lt;/a&gt; method to get a reference to the output &lt;code class=&quot;highlighter-rouge&quot;&gt;Tensors&lt;/code&gt; from the model. Since they are just references, we don’t need to manually dispose of them.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\update_method_processoutput.png&quot; alt=&quot;update_method_processoutput&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Now we can start filling out the &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt; method.&lt;/p&gt;

&lt;h2 id=&quot;calculate-scaling-values&quot;&gt;Calculate Scaling Values&lt;/h2&gt;

&lt;p&gt;The heatmaps generated by the model are much smaller than the input image fed into it. We’ll need to make some calculations to accurately scale the key point locations back up to the source resolution.&lt;/p&gt;

&lt;h3 id=&quot;calculate-model-stride&quot;&gt;Calculate Model Stride&lt;/h3&gt;

&lt;p&gt;The heatmap dimensions are dependent on both the size of the input image and a fixed integer value called the stride. The stride determines how much smaller the heatmaps will be than the input image. The model used in this tutorial has a stride of &lt;code class=&quot;highlighter-rouge&quot;&gt;32&lt;/code&gt;. The heatmap dimensions are equal to the &lt;a href=&quot;https://www.mathsisfun.com/sets/function-floor-ceiling.html&quot;&gt;ceiling&lt;/a&gt; of &lt;code class=&quot;highlighter-rouge&quot;&gt;resolution/stride&lt;/code&gt;. With our default input resolution of &lt;code class=&quot;highlighter-rouge&quot;&gt;360 x 360&lt;/code&gt;, the size of the heatmaps are &lt;code class=&quot;highlighter-rouge&quot;&gt;12 x 12&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;Since we know the stride for this model, we could make it a constant value. However, calculating it is an easy way to make sure. This also makes it less of a hassle when switching between models with different stride values.&lt;/p&gt;

&lt;h4 id=&quot;model-with-a-different-stride-value&quot;&gt;Model with a Different Stride Value&lt;/h4&gt;

&lt;ul&gt;
  &lt;li&gt;ResNet50 Stride 16: (&lt;a href=&quot;https://drive.google.com/file/d/1dlsWlBpjgD2AuZgi-qhZs-1IV-T98iLM/view?usp=sharing&quot;&gt;download&lt;/a&gt;)&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;To get the stride value, we’ll select a dimension of &lt;code class=&quot;highlighter-rouge&quot;&gt;inputImage&lt;/code&gt; and subtract &lt;code class=&quot;highlighter-rouge&quot;&gt;1&lt;/code&gt;. We then divide that value by the same dimension of the heatmap with &lt;code class=&quot;highlighter-rouge&quot;&gt;1&lt;/code&gt; subtracted as well. If we don’t subtract &lt;code class=&quot;highlighter-rouge&quot;&gt;1&lt;/code&gt;, we’ll undershoot the stride value.&lt;/p&gt;

&lt;p&gt;For most input resolutions this will yield a value that is slightly above the actual stride. If we left it there, the key point locations would be offset from the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;. To compensate, we’ll subtract the remainder of the calculated stride divided by &lt;code class=&quot;highlighter-rouge&quot;&gt;8&lt;/code&gt;. The stride for the PoseNet models provided in this tutorial series are all multiples of &lt;code class=&quot;highlighter-rouge&quot;&gt;8&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\stride.png&quot; alt=&quot;stride&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;calculate-image-scale&quot;&gt;Calculate Image Scale&lt;/h3&gt;

&lt;p&gt;After scaling the output back to the &lt;code class=&quot;highlighter-rouge&quot;&gt;inputImage&lt;/code&gt; resolution, we’ll need to scale the output up to the source resolution. We can use the dimensions of &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; to calculate this scale.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\scale.png&quot; alt=&quot;scale&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;calculate-aspect-ratio-scale&quot;&gt;Calculate Aspect Ratio Scale&lt;/h3&gt;

&lt;p&gt;As I noted in &lt;a href=&quot;https://christianjmills.com/unity/tutorial/2020/11/04/Barracuda-PoseNet-Tutorial-2.html#resize-the-image&quot;&gt;Part 2&lt;/a&gt;, we need to compensate for the change in aspect ratio that results from resizing the image. We can use the dimensions of the &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; to stretch the output to the original aspect ratio.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\unsqueezeScale.png&quot; alt=&quot;unsqueezeScale&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;iterate-through-heatmaps&quot;&gt;Iterate Through Heatmaps&lt;/h2&gt;

&lt;p&gt;Now we can iterate through each of the heatmaps and determine the location of the associated key points.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\iterate_through_heatmaps_2.png&quot; alt=&quot;iterate_through_heatmaps&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;locate-key-point-indices&quot;&gt;Locate Key Point Indices&lt;/h3&gt;

&lt;p&gt;For each heatmap, we’ll first need to locate the index with the  highest confidence value. This indicates what region of the image the model thinks is most likely to contain that key point. We’ll create a separate method to handle this.&lt;/p&gt;

&lt;p&gt;The new method will be called &lt;code class=&quot;highlighter-rouge&quot;&gt;LocateKeyPointIndex()&lt;/code&gt; and take in the &lt;code class=&quot;highlighter-rouge&quot;&gt;heatmaps&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;offsets&lt;/code&gt; tensors along with the current &lt;code class=&quot;highlighter-rouge&quot;&gt;keypointIndex&lt;/code&gt;. It will return a &lt;a href=&quot;https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/builtin-types/value-tuples&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Tuple&lt;/code&gt;&lt;/a&gt; containing the &lt;code class=&quot;highlighter-rouge&quot;&gt;(x,y)&lt;/code&gt; coordinates from the heatmap index, the associated offset vector, and the confidence value at the heatmap index.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\locateKeyPointIndex_method.png&quot; alt=&quot;locateKeyPointIndex_method&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;call-the-method&quot;&gt;Call the Method&lt;/h4&gt;

&lt;p&gt;We’ll call &lt;code class=&quot;highlighter-rouge&quot;&gt;LocateKeyPointIndex()&lt;/code&gt; at the start of each iteration through the for loop in &lt;code class=&quot;highlighter-rouge&quot;&gt;ProcessOutput()&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\processOutput_locateIndices_2.png&quot; alt=&quot;processOutput_locateIndices&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;calculate-key-point-positions&quot;&gt;Calculate Key Point Positions&lt;/h3&gt;

&lt;p&gt;Now we can calculate the estimated key point locations relative to the source &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;. We’ll first extract the output  from the &lt;code class=&quot;highlighter-rouge&quot;&gt;Tuple&lt;/code&gt; returned by &lt;code class=&quot;highlighter-rouge&quot;&gt;LocateKeyPointIndex()&lt;/code&gt;. The offset vectors are based on the &lt;code class=&quot;highlighter-rouge&quot;&gt;inputImage&lt;/code&gt; resolution so we’ll scale the &lt;code class=&quot;highlighter-rouge&quot;&gt;(x,y)&lt;/code&gt; coordinates by the &lt;code class=&quot;highlighter-rouge&quot;&gt;stride&lt;/code&gt; before adding them. We’ll then scale the coordinates up to the source &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;Only the x-axis position is scaled by the &lt;code class=&quot;highlighter-rouge&quot;&gt;unsqueezeValue&lt;/code&gt;. This is specific to our current &lt;code class=&quot;highlighter-rouge&quot;&gt;videoTexture&lt;/code&gt; aspect ratio. I will cover a more dynamic approach in a later post.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\calculate_position_2.png&quot; alt=&quot;calculate_position&quot; /&gt;&lt;/p&gt;

&lt;h4 id=&quot;store-key-point-positions&quot;&gt;Store Key Point Positions&lt;/h4&gt;

&lt;p&gt;Finally, we’ll store the location data for the current key point at the corresponding index in the &lt;code class=&quot;highlighter-rouge&quot;&gt;keypointLocations&lt;/code&gt; array.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-4\store_position_2.png&quot; alt=&quot;store_position&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;

&lt;p&gt;We finally have the estimated key point locations relative to the source video. However, we still don’t have an easy means to gauge the model’s accuracy. In the next post, we’ll map each key point location to a &lt;code class=&quot;highlighter-rouge&quot;&gt;GameObject&lt;/code&gt;. This will provide a quick way to determine if the model is outputting nonsense as well as what scenarios the model struggles with.&lt;/p&gt;

&lt;h3 id=&quot;next-part-5&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-5/&quot;&gt;Part 5&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 30, 2020</summary></entry><entry><title type="html">Barracuda PoseNet Tutorial Pt. 3</title><link href="https://christianjmills.com/Barracuda-PoseNet-Tutorial-3/" rel="alternate" type="text/html" title="Barracuda PoseNet Tutorial Pt. 3" /><published>2020-11-06T00:00:00-06:00</published><updated>2020-11-06T00:00:00-06:00</updated><id>https://christianjmills.com/Barracuda-PoseNet-Tutorial-3</id><content type="html" xml:base="https://christianjmills.com/Barracuda-PoseNet-Tutorial-3/">&lt;p&gt;&lt;strong&gt;Last Updated:&lt;/strong&gt; Nov 30, 2020&lt;/p&gt;

&lt;h3 id=&quot;previous-part-2-part-25&quot;&gt;Previous: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-2/&quot;&gt;Part 2&lt;/a&gt; &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-2-5/&quot;&gt;Part 2.5&lt;/a&gt;&lt;/h3&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;#introduction&quot;&gt;Introduction&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#install-barracuda-package&quot;&gt;Install Barracuda Package&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#import-posenet-model&quot;&gt;Import PoseNet Model&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#load-the-model&quot;&gt;Load the Model&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#set-inspector-variables&quot;&gt;Set Inspector Variables&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#perform-inference&quot;&gt;Perform Inference&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#summary&quot;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;introduction&quot;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;We can now start using the Barracuda library. We’ll first install the Barracuda package for our project and import the PoseNet model. We’re going to modify the model a bit to make postprocessing easier. Finally, we’ll perform inference using a preprocessed image as input.&lt;/p&gt;

&lt;h2 id=&quot;install-barracuda-package&quot;&gt;Install Barracuda Package&lt;/h2&gt;

&lt;p&gt;Select the &lt;code class=&quot;highlighter-rouge&quot;&gt;Package Manager&lt;/code&gt; tab in the Unity editor.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\select_package_manager_tab.png&quot; alt=&quot;select_package_manager_tab&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Type &lt;code class=&quot;highlighter-rouge&quot;&gt;Barracuda&lt;/code&gt; into the search box. The version of the package used in the tutorial is &lt;code class=&quot;highlighter-rouge&quot;&gt;1.0.4&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\barracuda_search.PNG&quot; alt=&quot;barracuda_search&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Click the &lt;code class=&quot;highlighter-rouge&quot;&gt;Install&lt;/code&gt; button to install the package.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\barracuda_install.PNG&quot; alt=&quot;barracuda_install&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Wait for Unity to install the dependencies.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\barracuda_installation_progress.PNG&quot; alt=&quot;barracuda_installation_progress&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;import-posenet-model&quot;&gt;Import PoseNet Model&lt;/h2&gt;

&lt;p&gt;Now we can import the model into Unity. The Barracuda dev team has focused on supporting the &lt;a href=&quot;https://onnx.ai/&quot;&gt;ONNX&lt;/a&gt; format for models. We aren’t able to directly import models from TensorFlow or PyTorch. I’ve already converted the PoseNet model to ONNX. You can check out my tutorial for converting TensorFlow SavedModels to ONNX (&lt;a href=&quot;https://christianjmills.com/tensorflow/onnx/tutorial/2020/10/21/How-to-Convert-a-TensorFlow-SavedModel-to-ONNX.html&quot;&gt;here&lt;/a&gt;). PyTorch provides built-in support for ONNX (&lt;a href=&quot;https://pytorch.org/tutorials/advanced/super_resolution_with_onnxruntime.html&quot;&gt;link&lt;/a&gt;).&lt;/p&gt;

&lt;h3 id=&quot;download-the-onnx-file&quot;&gt;Download the ONNX File&lt;/h3&gt;

&lt;p&gt;You can download the converted PoseNet model from the link below.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;ResNet50: (&lt;a href=&quot;https://drive.google.com/file/d/1oKrlraI3m3ecme-pAvAh25-Jzzu86sv_/view?usp=sharing&quot;&gt;download&lt;/a&gt;)&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&quot;import-model-to-assets&quot;&gt;Import Model to Assets&lt;/h3&gt;

&lt;p&gt;Create a new folder in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Assets&lt;/code&gt; window and name it &lt;code class=&quot;highlighter-rouge&quot;&gt;Models&lt;/code&gt;. Drag and drop the ONNX file into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Models&lt;/code&gt; folder.&lt;/p&gt;

&lt;p&gt;If you select the &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet50&lt;/code&gt; asset, you should see the following in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\resnet50_inspector_tab.PNG&quot; alt=&quot;resnet50_inspector_tab&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;load-the-model&quot;&gt;Load the Model&lt;/h2&gt;

&lt;p&gt;Next, we need to implement the code for loading the model in the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; &lt;a href=&quot;https://christianjmills.com/unity/tutorial/2020/11/04/Barracuda-PoseNet-Tutorial-2.html#create-the-posenet-script&quot;&gt;script&lt;/a&gt;.&lt;/p&gt;

&lt;h3 id=&quot;add-unitybarracuda-namespace&quot;&gt;Add &lt;code class=&quot;highlighter-rouge&quot;&gt;Unity.Barracuda&lt;/code&gt; Namespace&lt;/h3&gt;

&lt;p&gt;Open the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script and add the &lt;code class=&quot;highlighter-rouge&quot;&gt;Unity.Barracuda&lt;/code&gt; namespace at the top of the script.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\add_barracuda_namespace.png&quot; alt=&quot;add_barracuda_namespace&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-modelasset-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;modelAsset&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Make a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;NNModel&lt;/code&gt; variable called &lt;code class=&quot;highlighter-rouge&quot;&gt;modelAsset&lt;/code&gt;. We’ll assign the &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet50&lt;/code&gt; asset to this variable in the Unity Editor.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\create_modelAsset_variable.png&quot; alt=&quot;create_modelAsset_variable&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-workertype-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;workerType&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We’ll also add a variable that let’s us choose which &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/manual/Worker.html&quot;&gt;backend&lt;/a&gt; to use when performing inference. The options are divided into &lt;code class=&quot;highlighter-rouge&quot;&gt;CPU&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;GPU&lt;/code&gt;. I believe there are plans to add support for specialized hardware such as Neural Processing Units in the future. Our preprocessing pipeline runs entirely on the &lt;code class=&quot;highlighter-rouge&quot;&gt;GPU&lt;/code&gt; so we’ll be sticking with the &lt;code class=&quot;highlighter-rouge&quot;&gt;GPU&lt;/code&gt; options for this tutorial series.&lt;/p&gt;

&lt;p&gt;Make a new public &lt;code class=&quot;highlighter-rouge&quot;&gt;WorkerFactory.Type&lt;/code&gt; called &lt;code class=&quot;highlighter-rouge&quot;&gt;workerType&lt;/code&gt;. Give it a default value of &lt;code class=&quot;highlighter-rouge&quot;&gt;WorkerFactory.Type.Auto&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\create_workerType_variable.png&quot; alt=&quot;load_model_variables_1&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-m_runtimemodel-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;m_RuntimeModel&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We need to compile the &lt;code class=&quot;highlighter-rouge&quot;&gt;modelAsset&lt;/code&gt; into a run-time model to perform inference. We’ll store the compiled model in a new private &lt;code class=&quot;highlighter-rouge&quot;&gt;Model&lt;/code&gt; variable called &lt;code class=&quot;highlighter-rouge&quot;&gt;m_RuntimeModel&lt;/code&gt;. This is the naming convention used in the Barracuda &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/manual/Loading.html&quot;&gt;documentation&lt;/a&gt;.&lt;/p&gt;

&lt;h3 id=&quot;create-engine-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;engine&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Next, we’ll create a new private &lt;code class=&quot;highlighter-rouge&quot;&gt;IWorker&lt;/code&gt; variable to store our inference engine. Name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;engine&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\load_model_variables_2.png&quot; alt=&quot;load_model_variables_2&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;create-heatmaplayer-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;heatmapLayer&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;Add a new private &lt;code class=&quot;highlighter-rouge&quot;&gt;string&lt;/code&gt; variable to store the  name of the heatmap layer in the &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet50&lt;/code&gt; model. We’ll need the output of this layer to determine the location of key points (e.g. nose, elbows, knees, etc.) in the input image. We can find the name for the model’s output layers in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Inspector&lt;/code&gt; tab. For our model, the heatmap layer is named &lt;code class=&quot;highlighter-rouge&quot;&gt;float_heamap&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\resnet50_output_layers.PNG&quot; alt=&quot;resnet50_output_layers&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; The last two output layers, &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet_v1_50/displacement_bwd_2/BiasAd&lt;/code&gt; and &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet_v1_50/displacement_fwd_2/BiasAd&lt;/code&gt;, are used when estimating the pose of multiple people. We’ll be sticking to single pose estimation for this series.&lt;/p&gt;

&lt;h3 id=&quot;create-offsetslayer-variable&quot;&gt;Create &lt;code class=&quot;highlighter-rouge&quot;&gt;offsetsLayer&lt;/code&gt; Variable&lt;/h3&gt;

&lt;p&gt;We’ll go ahead and create a variable for the &lt;code class=&quot;highlighter-rouge&quot;&gt;float_short_offsets&lt;/code&gt; layer as well since we’ll need it later. The output from this layer is used to refine the estimated key point locations determined with the heatmap layer.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\layer_name_variables.png&quot; alt=&quot;layer_name_variables&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;compile-the-model&quot;&gt;Compile the Model&lt;/h3&gt;

&lt;p&gt;We need to get an object oriented representation of the model before we can work with it. We’ll do this in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Start()&lt;/code&gt; method and store it in the &lt;code class=&quot;highlighter-rouge&quot;&gt;m_RuntimeModel&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\compile_model.png&quot; alt=&quot;compile_model&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;modify-the-model&quot;&gt;Modify the Model&lt;/h3&gt;

&lt;p&gt;We need to add a &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.ModelBuilder.html#Unity_Barracuda_ModelBuilder_Sigmoid_System_String_System_Object_&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Sigmoid&lt;/code&gt;&lt;/a&gt; layer to the end of the model before creating our inference engine. This will map the output values to the range &lt;code class=&quot;highlighter-rouge&quot;&gt;[0,1]&lt;/code&gt;. We’ll use these values to measure the model’s confidence that a given key point is in a given spot in the input image. A value of &lt;code class=&quot;highlighter-rouge&quot;&gt;1&lt;/code&gt; would indicate that the model is &lt;code class=&quot;highlighter-rouge&quot;&gt;100%&lt;/code&gt; confident the key point is in that location. We won’t be getting any &lt;code class=&quot;highlighter-rouge&quot;&gt;1&lt;/code&gt;’s.&lt;/p&gt;

&lt;p&gt;First, we need to make a new private &lt;code class=&quot;highlighter-rouge&quot;&gt;string&lt;/code&gt; variable to store the name of this new layer. We’ll name the variable &lt;code class=&quot;highlighter-rouge&quot;&gt;predictionLayer&lt;/code&gt; and name the layer &lt;code class=&quot;highlighter-rouge&quot;&gt;heatmap_predictions&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\predictionLayer_name.png&quot; alt=&quot;predictionLayer_name&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We’ll add the new layer using a &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.ModelBuilder.html&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;ModelBuilder&lt;/code&gt;&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\add_sigmoid_layer.png&quot; alt=&quot;add_sigmoid_layer&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;initialize-the-inference-engine&quot;&gt;Initialize the Inference Engine&lt;/h3&gt;

&lt;p&gt;Now we can create a worker to execute the modified model using the selected backend. We’ll do this using the &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.WorkerFactory.html#Unity_Barracuda_WorkerFactory_CreateWorker_Unity_Barracuda_WorkerFactory_Type_Unity_Barracuda_Model_System_Boolean_&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;WorkerFactory.CreateWorker()&lt;/code&gt;&lt;/a&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\create_worker.png&quot; alt=&quot;create_worker&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;release-inference-engine-resources&quot;&gt;Release Inference Engine Resources&lt;/h3&gt;

&lt;p&gt;We need to manually release the resources that get allocated for the inference &lt;code class=&quot;highlighter-rouge&quot;&gt;engine&lt;/code&gt;. This should be one of the last actions performed. Therefore, we’ll do it in the &lt;code class=&quot;highlighter-rouge&quot;&gt;OnDisable()&lt;/code&gt; method. This method gets called when the Unity project exits. We need to implement this method in the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseNet&lt;/code&gt; script.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\onDisable_method.png&quot; alt=&quot;onDisable_method&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;set-inspector-variables&quot;&gt;Set Inspector Variables&lt;/h2&gt;

&lt;p&gt;Now we just need to set the values for the &lt;code class=&quot;highlighter-rouge&quot;&gt;Model Asset&lt;/code&gt; and select the inference backend.&lt;/p&gt;

&lt;h3 id=&quot;assign-the-model-asset&quot;&gt;Assign the Model Asset&lt;/h3&gt;

&lt;p&gt;With the &lt;code class=&quot;highlighter-rouge&quot;&gt;PoseEstimator&lt;/code&gt; object selected, drag and drop the &lt;code class=&quot;highlighter-rouge&quot;&gt;resnet50&lt;/code&gt; asset into the &lt;code class=&quot;highlighter-rouge&quot;&gt;Model Asset&lt;/code&gt; variable.&lt;/p&gt;

&lt;h3 id=&quot;select-inference-backend&quot;&gt;Select Inference Backend&lt;/h3&gt;

&lt;p&gt;Set the backend to the &lt;code class=&quot;highlighter-rouge&quot;&gt;Compute Precompiled&lt;/code&gt; option in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Worker Type&lt;/code&gt; drop-down. This is the most efficient GPU backend.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\assign_model_asset_and_backend.PNG&quot; alt=&quot;assign_model_asset_and_backend&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;perform-inference&quot;&gt;Perform Inference&lt;/h2&gt;

&lt;p&gt;Finally, we’ll add the code to perform inference in the &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt; method.&lt;/p&gt;

&lt;h3 id=&quot;create-the-input-tensor&quot;&gt;Create the &lt;code class=&quot;highlighter-rouge&quot;&gt;input&lt;/code&gt; Tensor&lt;/h3&gt;

&lt;p&gt;We need to convert the &lt;code class=&quot;highlighter-rouge&quot;&gt;processedImage&lt;/code&gt; to a &lt;code class=&quot;highlighter-rouge&quot;&gt;Tensor&lt;/code&gt; before we can feed it to the model. The &lt;code class=&quot;highlighter-rouge&quot;&gt;Tensor&lt;/code&gt; constructor requires us to specify the number of channels in the image. We don’t need the alpha (transparency) channel so we’ll specify &lt;code class=&quot;highlighter-rouge&quot;&gt;3&lt;/code&gt; for the RGB color channels.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\create_input_tensor.png&quot; alt=&quot;create_input_tensor&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;execute-the-model&quot;&gt;Execute the Model&lt;/h3&gt;

&lt;p&gt;We’ll use the &lt;a href=&quot;https://docs.unity3d.com/Packages/com.unity.barracuda@1.0/api/Unity.Barracuda.IWorker.html#Unity_Barracuda_IWorker_Execute_Unity_Barracuda_Tensor_&quot;&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;engine.Execute()&lt;/code&gt;&lt;/a&gt; method to perform inference. This method takes in the input Tensor and schedules the network execution.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\execute_model.png&quot; alt=&quot;execute_model&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;release-input-tensor-resources&quot;&gt;Release Input Tensor Resources&lt;/h3&gt;

&lt;p&gt;We’ll need to manually release the allocated resources for the Tensor with the &lt;code class=&quot;highlighter-rouge&quot;&gt;input.Dispose()&lt;/code&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\dispose_input_tensor.png&quot; alt=&quot;dispose_input_tensor&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Here is the revised &lt;code class=&quot;highlighter-rouge&quot;&gt;Update()&lt;/code&gt; method.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;\images\barracuda-posenet-tutorial\part-3\perform_inference_update_method_3.png&quot; alt=&quot;perform_inference_update_method&quot; /&gt;&lt;/p&gt;

&lt;h2 id=&quot;summary&quot;&gt;Summary&lt;/h2&gt;

&lt;p&gt;We’ve finally performed inference using a PoseNet model in Unity. However, we need to process the output from the model to determine the estimated key point locations. We’ll cover how to perform the postprocessing operations in part 4.&lt;/p&gt;

&lt;h3 id=&quot;next-part-4&quot;&gt;Next: &lt;a href=&quot;https://christianjmills.com/Barracuda-PoseNet-Tutorial-4/&quot;&gt;Part 4&lt;/a&gt;&lt;/h3&gt;</content><author><name></name></author><summary type="html">Last Updated: Nov 30, 2020</summary></entry></feed>